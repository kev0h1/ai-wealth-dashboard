# Dependencies
frontend/node_modules/
backend/.venv/
backend/venv/

# Build output
frontend/.next/

# Secrets & local config
backend/.env
backend/.session_secret
backend/.vapid_private_key
backend/.webhook_secret
backend/.token_key
backend/.finexer_webhook_secret
backend/.finexer_webhook_signing_secret
backend/.apns_auth_key.p8
# Apple ships APNs auth keys and App Store Connect API keys as AuthKey_*.p8 files, and either one is a live credential.
# The line above documents where the backend expects to find its key, this pattern catches any copy anywhere in the repo.
*.p8
backend/.fcm_service_account.json
*service-account*.json
*service_account*.json
*firebase-adminsdk*.json
backend/store.json

# Python cache
backend/__pycache__/
*.pyc

# OS
.DS_Store

# local SSH keys for throwaway hosts — never commit
.keys/

# cached fuel-price snapshots (collected via UK egress)
backend/fuel_cache/

# cached merchant logo PNGs (proxied from Clearbit)
backend/.logo_cache/
backups/

# Local Claude / tooling artifacts — never commit
.claude/
.impeccable/
.mcp.json
.railway-config-pull-*/
backend/bot_state.json
skills-lock.json

# Mobile / Capacitor spike artifacts — never commit or deploy
.android-sdk/
capacitor-spike/node_modules/
capacitor-spike/www/
capacitor-spike/android/
capacitor-spike/ios/
# Firebase config, per-package, never commit
capacitor-spike/google-services.json
# Firebase config, per-package, never commit
mobile/google-services.json
frontend/public/*.apk
capacitor-spike/dist/
frontend/out/
mobile/node_modules/

# Built APKs — never commit binaries
*.apk

# Ad-hoc design-review scratch dir (LLM critiques, not for commit)
design-review/

frontend/.screens/
