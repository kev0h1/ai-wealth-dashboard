// Bumped by hand on every mobile (Capacitor) APK rebuild that's handed to
// Kevin for testing. Rendered as a tiny whisper on the login screen and the
// biometric lock screen so he can visually confirm, from a screenshot or
// just eyeballing the phone, which build is actually running — critical
// after the biometric-lock-loop incident, where re-publishing an APK under
// the same filename made it impossible to tell whether a stuck phone was
// running the old broken build or a stale download of it.
export const BUILD_TAG = "build 2026-08-18c";
