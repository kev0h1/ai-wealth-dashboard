"use client";
import Link from "next/link";
import {useSearchParams} from "next/navigation";
import StoryPlayer from "@/app/month/story/StoryPlayer";
import {MONTH_STORY_FIXTURE} from "../month-story/fixtures";
type V="a"|"b"|"c";const variants:V[]=["a","b","c"];const props={a:{spotlight:"anchored" as const,align:"default" as const,label:"Anchored story"},b:{spotlight:"none" as const,align:"center" as const,label:"Quiet centre"},c:{spotlight:"tight" as const,align:"center" as const,label:"Close focus"}};
export default function Client(){const p=useSearchParams(),raw=p.get("variant"),v:V=raw==="b"||raw==="c"?raw:"a",cfg=props[v];return <div style={{colorScheme:"dark"}}><a href="/month" className="fixed left-4 top-4 z-[70] inline-flex min-h-11 items-center rounded-xl bg-slate-950/80 px-3 text-sm font-semibold text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white">Return to month close</a><StoryPlayer fixtureStory={MONTH_STORY_FIXTURE} spotlight={cfg.spotlight} align={cfg.align}/><nav aria-label="G99 design variants" className="fixed inset-x-0 bottom-[calc(env(safe-area-inset-bottom)+14px)] z-[70] flex justify-center px-3"><div className="flex max-w-full overflow-x-auto rounded-full border border-white/15 bg-slate-950/90 p-1 shadow-xl">{variants.map(x=><Link key={x} href={`?variant=${x}`} className={`inline-flex min-h-11 shrink-0 items-center justify-center rounded-full px-3 text-xs font-semibold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white ${x===v?"bg-indigo-600 text-white":"text-slate-300"}`}>{x.toUpperCase()} · {props[x].label}</Link>)}</div></nav></div>}
