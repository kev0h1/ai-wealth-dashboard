"use client";
import{useEffect,useState}from"react";import{useSearchParams}from"next/navigation";import{ArrowLeft,ChevronRight,LogOut,ShieldCheck}from"lucide-react";
type V="a"|"b"|"c";type S="ready"|"attention"|"empty";const groups:[string,[string,string][]][]=[['Account and plan',[['Profile','Alex Morgan'],['Your plan','Standard · £5.99/month'],['Sign-in methods','Google & Apple']]],['Permissions and security',[['App lock','Face ID on'],['Connected accounts','8 accounts'],['Notifications','Bills and payday reminders']]],['How Sorted behaves',[['Display','Dark mode on'],['Penny','Acts with confirmation'],['Your data','Export or delete']]]];
function List({attention,order}:{attention:boolean;order:number[]}){const[o,setO]=useState<string|null>(null);return <div className="space-y-7">{order.map(i=>{const[g,rows]=groups[i];return <section key={g}><h2 className="text-base font-bold">{g}</h2><div className="mt-3 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-800">{rows.map(([name,value])=><div key={name} className="border-b last:border-0 dark:border-slate-700"><button type="button" onClick={()=>setO(o===name?null:name)} className="flex min-h-[68px] w-full items-center gap-3 px-4 text-left transition-colors hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-indigo-500 dark:hover:bg-slate-700"><span className="min-w-0 flex-1"><b className="flex items-center gap-2 text-sm">{name}{attention&&name==='Notifications'&&<i className="size-1.5 rounded-full bg-amber-500" aria-label="Needs attention"/>}</b><span className="text-xs text-slate-600 dark:text-slate-400">{attention&&name==='Notifications'?'Permission needs attention':value}</span></span><ChevronRight aria-hidden="true" size={16}/></button>{o===name&&<p className="border-t px-4 py-3 text-sm text-slate-600 dark:border-slate-700 dark:text-slate-400">Preview detail for {name}. No setting changes here.</p>}</div>)}</div></section>})}</div>}
function Exit(){const[state,setState]=useState<'idle'|'confirm'|'done'>('idle');return <section className="mt-9 border-t pt-6"><h2 className="font-bold">Leave Sorted</h2>{state==='idle'&&<button type="button" onClick={()=>setState('confirm')} className="mt-3 inline-flex min-h-11 items-center gap-2 rounded-xl border px-4 text-sm font-semibold transition-colors hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 dark:hover:bg-slate-800"><LogOut aria-hidden="true" size={16}/>Sign out</button>}{state==='confirm'&&<div className="mt-3 rounded-2xl border border-slate-200 bg-white p-4 dark:border-slate-700 dark:bg-slate-800"><p className="text-sm font-semibold">Sign out from this preview?</p><p className="mt-1 text-sm text-slate-600 dark:text-slate-400">This only changes the preview state.</p><div className="mt-3 flex gap-2"><button type="button" onClick={()=>setState('done')} className="min-h-11 rounded-xl bg-indigo-600 px-4 text-sm font-semibold text-white hover:bg-indigo-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500">Confirm sign out</button><button type="button" onClick={()=>setState('idle')} className="min-h-11 rounded-xl border px-4 text-sm font-semibold hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 dark:hover:bg-slate-700">Cancel</button></div></div>}{state==='done'&&<p className="mt-3 text-sm text-slate-600 dark:text-slate-400">You have signed out of this preview. No real session ended.</p>}</section>}
export default function G94SettingsCanvasClient(){const p=useSearchParams(),v:V=p.get('variant')==='b'||p.get('variant')==='c'?p.get('variant')as V:'a',s:S=p.get('state')==='attention'||p.get('state')==='empty'?p.get('state')as S:'ready',d=p.get('mode')==='dark';useEffect(()=>{document.documentElement.classList.toggle('dark',d)},[d]);const order=v==='a'?[0,1,2]:v==='b'?[1,0,2]:[2,0,1],intro=v==='a'?'Manage your account and plan first.':v==='b'?'Review access and permissions before changing preferences.':'Change how Sorted behaves, then review account access.';return <div className={`${d?'dark':''} min-h-dvh bg-[#f0f2f7] pb-28 text-slate-900 dark:bg-[#0f172a] dark:text-slate-100`}><main className="mx-auto max-w-5xl px-4 py-8"><a href="/design" className="inline-flex min-h-11 items-center gap-1 text-sm text-slate-600 hover:text-slate-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 dark:text-slate-400 dark:hover:text-white"><ArrowLeft aria-hidden="true" size={16}/>Back to design</a><header className="mt-4 border-b pb-6"><h1 className="text-3xl font-bold">Settings</h1><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">{intro}</p>{s==='attention'&&<p className="mt-3 flex gap-2 text-sm"><ShieldCheck aria-hidden="true" className="text-amber-600" size={17}/>Notification permission needs your attention.</p>}</header><div className="mt-8">{s==='empty'?<section><h2 className="text-xl font-bold">Choose what to set up</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">You can manage sign-in and privacy now, then connect a bank when you are ready.</p></section>:<><List attention={s==='attention'} order={order}/><Exit/></>}</div></main><nav aria-label="Preview controls" className="fixed inset-x-0 bottom-3 flex justify-center px-2"><div className="flex max-w-full overflow-x-auto rounded-2xl bg-slate-900/95 p-1 text-xs">{(['a','b','c']as V[]).map(x=><a key={x} href={`?variant=${x}&state=${s}&mode=${d?'dark':'light'}`} className="min-h-11 px-3 py-3 text-slate-200">{x.toUpperCase()}</a>)}<a href={`?variant=${v}&state=${s}&mode=${d?'light':'dark'}`} className="min-h-11 px-3 py-3 text-slate-200">{d?'light':'dark'}</a></div></nav></div>}
