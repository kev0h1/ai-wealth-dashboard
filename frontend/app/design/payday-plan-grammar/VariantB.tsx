import type { PaydayScenario } from "./fixtures";
import { DestinationList, DownArrow, PaydayCard, PreviewHeading, Remaining, SalarySource } from "./shared";

export default function VariantB({ scenario }: { scenario: PaydayScenario }) { return <section aria-label="Variant B, allocation ledger"><PreviewHeading title="B · Allocation ledger" copy="Every allocation stays visible: the clearest audit trail from expected pay to each account." /><PaydayCard scenario={scenario}><div className="mt-3 rounded-xl border border-slate-100 bg-slate-50/70 px-3 dark:border-slate-700 dark:bg-slate-900/30"><SalarySource salary={scenario.salary} /><div className="flex justify-center"><DownArrow /></div><div data-destination-summary><p className="pt-1 text-[10px] font-semibold uppercase tracking-[0.05em] text-slate-400 dark:text-slate-500">Moving to {scenario.destinations.length} accounts</p><DestinationList destinations={scenario.destinations} /></div></div><Remaining scenario={scenario} /></PaydayCard></section>; }
