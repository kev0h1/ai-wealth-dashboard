"use client";
import {useEffect,useState} from "react";import {useSearchParams} from "next/navigation";import {AlertCircle,ArrowLeft,ChevronDown,FileImage,ReceiptText} from "lucide-react";
type V="a"|"b"|"c";type S="ready"|"empty"|"loading"|"error";const baskets=[['sainsburys','Sainsbury’s','Today · 12 items','£42.18'],['tesco','Tesco','Yesterday · 8 items','£28.64'],['waitrose','Waitrose','Mon 8 Sept · 14 items','£56.10']]as const;
export default function G96ReceiptsCanvasClient(){const p=useSearchParams(),raw=p.get('variant'),v:V=raw==='b'||raw==='c'?raw:'a',rs=p.get('state'),s:S=rs==='empty'||rs==='loading'||rs==='error'?rs:'ready',d=p.get('mode')==='dark',[open,setOpen]=useState<string|null>(null);useEffect(()=>{document.documentElement.classList.toggle('dark',d)},[d]);const list=<section aria-labelledby="baskets"><h2 id="baskets" className="text-base font-bold">Scanned baskets</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-400">Open a basket to see the items that were read from its receipt.</p><div className="mt-3 overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-slate-700 dark:bg-slate-800">{baskets.map(([id,shop,meta,amount])=><div key={id} className="border-b border-slate-100 last:border-0 dark:border-slate-700"><button type="button" aria-expanded={open===id} aria-controls={`basket-${id}`} onClick={()=>setOpen(open===id?null:id)} className="flex min-h-[76px] w-full items-center gap-3 px-4 text-left hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-indigo-500 dark:hover:bg-slate-700"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-emerald-50 text-emerald-600 dark:bg-emerald-400/10 dark:text-emerald-300"><ReceiptText size={17}/></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold">{shop}</span><span className="block truncate text-xs text-slate-600 dark:text-slate-400">{meta}</span></span><span className="font-mono text-sm font-bold">{amount}</span><ChevronDown size={16} className={`text-slate-400 dark:text-slate-300 ${open===id?'rotate-180':''}`}/></button>{open===id&&<div id={`basket-${id}`} className="border-t border-slate-100 px-4 py-3 text-sm text-slate-600 dark:border-slate-700 dark:text-slate-300">Basket items: apples, pasta, milk and other scanned groceries.</div>}</div>)}</div></section>;
const content=s==='loading'?<div aria-label="Loading receipts" className="mt-8 space-y-3"><div className="h-24 animate-pulse rounded-2xl bg-slate-200 dark:bg-slate-800"/><div className="h-40 animate-pulse rounded-2xl bg-slate-200 dark:bg-slate-800"/></div>:s==='empty'?<section className="mt-10 border-y border-slate-200 py-8 dark:border-slate-700"><h2 className="text-xl font-bold">No receipts yet</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Scan a receipt in the app to start tracking grocery prices.</p></section>:s==='error'?<section className="mt-10 border-y border-slate-200 py-8 dark:border-slate-700"><h2 className="flex gap-2 text-xl font-bold"><AlertCircle className="text-slate-500"/>Couldn’t read your receipt</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Try another photo with the full receipt in good light. Existing baskets are safe.</p></section>:<div className={v==='b'?"grid gap-8 lg:grid-cols-[.75fr_1.25fr]":'space-y-8'}><section><p className="text-[11px] font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Your grocery baskets</p><h2 className="mt-2 text-xl font-bold">3 scanned baskets</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Each basket holds the groceries read from one receipt. Open one when you want the detail.</p></section>{v==='c'&&<p className="border-y border-slate-200 py-4 text-sm text-slate-600 dark:border-slate-700 dark:text-slate-400">A receipt becomes a basket, then its items stay together for later comparison.</p>}{list}</div>;return <main className={`${d?'dark':''} min-h-dvh bg-[#f0f2f7] pb-32 text-slate-900 dark:bg-[#0f172a] dark:text-slate-100`}><div className="mx-auto max-w-5xl px-4 py-8"><a href="/design" className="inline-flex min-h-11 items-center gap-1 text-sm focus-visible:ring-2"><ArrowLeft size={16}/>Back to design</a><h1 className="mt-4 text-3xl font-bold">Receipts</h1><div className="mt-8">{content}</div><p className="mt-10 flex gap-2 text-xs text-slate-600 dark:text-slate-400"><FileImage size={15}/>Static preview only. No receipt is uploaded or changed here.</p></div></main>}
