"use client";
import {useEffect,useState,type ReactNode}from"react";import{useSearchParams}from"next/navigation";import{AlertCircle,ArrowLeft,Camera,ChevronDown,ReceiptText,ScanLine}from"lucide-react";
type V="a"|"b"|"c";type S="ready"|"empty"|"loading"|"error";declare namespace React { type ReactNode = import("react").ReactNode };const rows=[['Sainsbury’s','Today · 12 items','£42.18'],['Tesco','Yesterday · 8 items','£28.64'],['Waitrose','Mon 8 Sept · 14 items','£56.10']];
function Capture(){const[q,setQ]=useState(false);return <section className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800"><div className="flex gap-3"><Camera className="mt-1 text-indigo-600"/><div><h2 className="text-base font-bold">Capture a receipt</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-400">Take a clear photo and Sorted will group the items into one basket for you to check.</p><button onClick={()=>setQ(!q)} className="mt-3 min-h-11 rounded-xl bg-indigo-600 px-4 text-sm font-semibold text-white">{q?'Photo queued in this fixture':'Use camera'}</button></div></div>{q&&<p className="mt-3 border-t pt-3 text-sm text-slate-600 dark:text-slate-400"><ScanLine size={15} className="mr-1 inline"/>Review the extracted basket before saving it.</p>}</section>}
function History(){const[o,setO]=useState<string|null>(null);return <section><h2 className="text-base font-bold">Receipt history</h2><div className="mt-3 overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-slate-700 dark:bg-slate-800">{rows.map(([shop,when,cost])=><div key={shop} className="border-b last:border-0 dark:border-slate-700"><button onClick={()=>setO(o===shop?null:shop)} className="flex min-h-[70px] w-full items-center gap-3 px-4 text-left"><ReceiptText className="text-emerald-600" size={18}/><span className="flex-1"><b className="block text-sm">{shop}</b><span className="text-xs text-slate-600 dark:text-slate-400">{when}</span></span><b className="font-mono text-sm">{cost}</b><ChevronDown size={16}/></button>{o===shop&&<p className="border-t px-4 py-3 text-sm text-slate-600 dark:text-slate-400">Basket items are confirmed together for later price comparison.</p>}</div>)}</div></section>}
export default function G96ReceiptsCanvasClient(){const p=useSearchParams(),v:V=p.get('variant')==='b'||p.get('variant')==='c'?p.get('variant')as V:'a',s:S=p.get('state')==='empty'||p.get('state')==='loading'||p.get('state')==='error'?p.get('state')as S:'ready',d=p.get('mode')==='dark',[ok,setOk]=useState(false);useEffect(()=>{document.documentElement.classList.toggle('dark',d)},[d]);const controls=<nav className="fixed inset-x-0 bottom-3 z-50 flex justify-center px-2"><div className="flex max-w-full overflow-x-auto rounded-2xl bg-slate-900/95 p-1 text-xs">{(['a','b','c']as V[]).map(x=><a key={x} href={`?variant=${x}&state=${s}&mode=${d?'dark':'light'}`} className="min-h-11 px-3 py-3 text-slate-200">{x.toUpperCase()}</a>)}<a href={`?variant=${v}&state=${s}&mode=${d?'light':'dark'}`} className="min-h-11 px-3 py-3 text-slate-200">{d?'light':'dark'}</a></div></nav>;let body:React.ReactNode;if(s==='empty')body=<><Capture/><section className="border-y py-7"><h2 className="text-xl font-bold">No receipts yet</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Your first captured receipt will become a basket here.</p></section></>;else if(s==='error')body=<><Capture/><section className="border-y py-7"><h2 className="flex gap-2 text-xl font-bold"><AlertCircle/>Couldn’t read this receipt</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Try another photo with the full receipt in good light. Existing baskets are safe.</p></section></>;else if(s==='loading')body=<div className="space-y-3"><div className="h-28 animate-pulse rounded-2xl bg-slate-200 dark:bg-slate-700"/><div className="h-40 animate-pulse rounded-2xl bg-slate-200 dark:bg-slate-700"/></div>;else body=<div className="space-y-8">{v==='b'?<div className="lg:grid lg:grid-cols-2 lg:gap-10"><div className="space-y-8"><Capture/><section className="border-y py-4"><h2 className="font-bold">Price insight</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-400">Pasta is £0.18 lower than your last basket. This is observed price history, not a saving promise.</p></section></div><div className="mt-8 lg:mt-0"><History/></div></div>:<><Capture/>{v==='a'&&<section><h2 className="font-bold">Ready to confirm</h2><button onClick={()=>setOk(!ok)} className="mt-3 min-h-11 rounded-xl border px-4 text-sm font-semibold">{ok?'Basket confirmed in this fixture':'Confirm basket'}</button></section>}{v==='c'&&<section className="border-y py-4"><h2 className="font-bold">One useful change</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-400">Your latest basket cost £13.92 less than the previous one, mostly from fewer branded items.</p></section>}<History/></>}</div>;return <div className={`${d?'dark':''} min-h-dvh bg-[#f0f2f7] pb-32 text-slate-900 dark:bg-[#0f172a] dark:text-slate-100`}><main className="mx-auto max-w-5xl px-4 py-8"><a href="/design" className="inline-flex min-h-11 items-center gap-1 text-sm"><ArrowLeft size={16}/>Back to design</a><header className="mt-4 border-b pb-6"><h1 className="text-3xl font-bold">Receipts</h1><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Capture, check the basket, then use the history when a price detail matters.</p></header><div className="mt-8">{body}</div><p className="mt-10 text-xs text-slate-600 dark:text-slate-400">Fixture only. No receipt is uploaded or changed here.</p></main>{controls}</div>}
