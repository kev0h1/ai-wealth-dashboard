"use client";
import{useEffect,useState}from"react";import{useSearchParams}from"next/navigation";import{AlertCircle,ChevronRight,Search,SlidersHorizontal}from"lucide-react";
type V="a"|"b"|"c";type S="populated"|"long"|"empty"|"loading"|"error";const base=[['Pret A Manger','Eating out','−£8.45',true],['Tesco','Groceries','−£42.18',false],['TfL Travel','Transport','−£3.20',true],['Spotify','Subscriptions','−£11.99',false],['Salary','Income','+£2,950.00',false]]as const;
function SearchPanel(){const[f,setF]=useState(false);return <div><label className="flex min-h-12 items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 shadow-sm focus-within:ring-2 focus-within:ring-indigo-500 dark:border-slate-700 dark:bg-slate-800"><Search aria-hidden="true" size={17}/><input aria-label="Search transactions" name="transaction-search" autoComplete="off" className="min-w-0 flex-1 bg-transparent text-sm outline-none" placeholder="Search payments, shops or amounts…"/><button type="button" onClick={()=>setF(!f)} aria-expanded={f} aria-label="Show filters" className="grid size-11 place-items-center rounded-lg hover:bg-slate-100 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 dark:hover:bg-slate-700"><SlidersHorizontal aria-hidden="true" size={17}/></button></label>{f&&<div className="mt-2 flex gap-2"><button type="button" onClick={()=>setF(false)} className="min-h-11 rounded-xl border px-3 text-xs font-semibold hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 dark:hover:bg-slate-700">This pay period</button><button type="button" onClick={()=>setF(false)} className="min-h-11 rounded-xl border px-3 text-xs font-semibold hover:bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 dark:hover:bg-slate-700">Needs review</button></div>}</div>}
function Ledger({evidence,long}:{evidence:boolean;long:boolean}){const[o,setO]=useState<string|null>(null);const[confirmed,setConfirmed]=useState<string|null>(null);const rows=long?Array.from({length:5},(_,i)=>base.map((x,j)=>[`${x[0]} · ${i+1}`,x[1],x[2],x[3],i===0?"Today":`Mon ${14-i} Sept`]as const)).flat():base.map(x=>[...x,'Today']as const);return <section><h2 className="text-base font-bold">Payments</h2><div className="mt-3 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-800">{rows.map(([name,category,amount,flag,day],i)=><div key={name} className="border-b last:border-0 dark:border-slate-700">{(i===0||day!==rows[i-1]?.[4])&&<p className="px-4 pt-3 text-xs font-semibold text-slate-500">{day}</p>}<button type="button" onClick={()=>setO(o===name?null:name)} className="flex min-h-[70px] w-full items-center gap-3 px-4 text-left"><span className="min-w-0 flex-1"><b className="flex gap-1.5 text-sm">{name}{flag&&confirmed!==name&&<i className="mt-1.5 size-1.5 rounded-full bg-amber-500" aria-label="Needs category confirmation"/>}</b><span className="text-xs text-slate-600 dark:text-slate-400">{flag&&confirmed!==name?'Suggested category needs confirmation':category}</span></span><b className="font-mono text-sm">{amount}</b><ChevronRight size={16} className={o===name?'rotate-90':''}/></button>{o===name&&<div className="border-t px-4 py-3 text-sm text-slate-600 dark:border-slate-700 dark:text-slate-400"><p><b className="text-slate-900 dark:text-white">Payment evidence</b> · {name} has {flag&&confirmed!==name?'a suggested':'the confirmed'} category {category}.</p>{flag&&confirmed!==name&&<button type="button" onClick={()=>setConfirmed(name)} className="mt-2 min-h-11 rounded-xl bg-indigo-600 px-3 text-xs font-semibold text-white">Confirm {category}</button>}{evidence&&<p className="mt-2">This detail is shown because you opened the payment.</p>}</div>}</div>)}</div></section>}
function Error(){const retry=()=>{const params=new URLSearchParams(window.location.search);params.set('state','populated');window.location.search=params.toString()};return <section className="border-y py-7"><h2 className="flex gap-2 text-xl font-bold"><AlertCircle aria-hidden="true"/>Transactions could not load</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Check your connection, then try again. No changes have been made.</p><button type="button" onClick={retry} className="mt-3 min-h-11 rounded-xl bg-indigo-600 px-4 text-sm font-semibold text-white hover:bg-indigo-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500">Try again</button></section>}
export default function TransactionsCanvasClient(){const p=useSearchParams(),v:V=p.get('variant')==='b'||p.get('variant')==='c'?p.get('variant')as V:'a',raw=p.get('state'),s:S=raw==='long'||raw==='empty'||raw==='loading'||raw==='error'?raw:'populated',d=p.get('mode')==='dark';useEffect(()=>{document.documentElement.classList.toggle('dark',d)},[d]);const workspace=s==='loading'?<div className="space-y-3"><div className="h-24 animate-pulse motion-reduce:animate-none rounded-2xl bg-slate-200 dark:bg-slate-700"/><div className="h-48 animate-pulse motion-reduce:animate-none rounded-2xl bg-slate-200 dark:bg-slate-700"/></div>:s==='empty'?<section className="border-y py-7"><h2 className="text-xl font-bold">No payments match this search</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Use the search or filters above to find another payment.</p></section>:s==='error'?<Error/>:v==='b'?<><section><h2 className="text-xl font-bold">Two payments need a category check</h2><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Confirm Pret A Manger and TfL Travel before relying on their category totals.</p></section><Ledger evidence={false} long={s==='long'}/></>:v==='c'?<><section className="border-y py-4"><h2 className="font-bold">Open a payment to see its evidence</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-400">Category teaching appears only when a payment needs confirmation.</p></section><Ledger evidence long={s==='long'}/></>:<Ledger evidence={false} long={s==='long'}/>;return <div className={`${d?'dark':''} min-h-dvh bg-[#f0f2f7] pb-28 text-slate-900 dark:bg-[#0f172a] dark:text-slate-100`}><main className="mx-auto max-w-5xl px-4 py-8"><header><h1 className="text-3xl font-bold">Transactions</h1><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">Find a payment, then check the evidence only when it matters.</p></header><div className="mt-6 space-y-7"><SearchPanel/>{workspace}</div></main><nav aria-label="Preview controls" className="fixed inset-x-0 bottom-3 flex justify-center px-2"><div className="flex max-w-full overflow-x-auto rounded-2xl bg-slate-900/95 p-1 text-xs">{(['a','b','c']as V[]).map(x=><a key={x} href={`?variant=${x}&state=${s}&mode=${d?'dark':'light'}`} className="min-h-11 px-3 py-3 text-slate-200">{x.toUpperCase()}</a>)}{(['populated','long','empty','loading','error']as S[]).map(x=><a key={x} href={`?variant=${v}&state=${x}&mode=${d?'dark':'light'}`} className="min-h-11 px-2 py-3 text-slate-200">{x}</a>)}<a href={`?variant=${v}&state=${s}&mode=${d?'light':'dark'}`} className="min-h-11 px-3 py-3 text-slate-200">{d?'light':'dark'}</a></div></nav></div>}
