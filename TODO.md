# Sorted backlog (written 2026-09-06)

Working rules for any session picking an item: branch `docs/mobile-porting-checkpoint`; UAT is this VPS (`systemctl restart wealth-api` / `wealth-worker` / `wealth-frontend` after `npm run build`); production is Vercel + Railway and is NOT touched by restarts here. Never commit `backend/.env` or any key file. Fable plans, Sonnet agents edit and commit, commit trailers `Co-Authored-By: Claude <model> <noreply@anthropic.com>`. No em dashes in user-facing copy. Design changes go through Kevin as coded variants on `/design/*` before touching production components. Tick items here as they land; add the commit hash.

Context documents: `docs/compliance/finexer-agent-controls-2026-09.md` (due diligence answers, `[KEVIN]` markers), `docs/pricing/tiering-unit-economics-mcp-2026-09.md` (tier table, unit economics, scaling, MCP design), `PENNY_TOOLS.md`, `SECURITY.md`, `PRIVACY.md`, `TERMS.md`, `DEPLOY.md`.

Board: the private page /ops/go-live on UAT reads and edits these files. Sessions use `scripts/backlog.py` (`start`, `block`, `done <id> --commit <sha>`, `note`) so the file, the page and git history stay one thing. Never tick by hand.

## A. Finexer go-live blockers (target 2026-10-01)

- [x] **A1. Production deploy of this branch.** [owner: claude] [priority: p1] [unblocks: Q5, Q6, Q7, Q10, Q11] (needs Kevin's approval to start) Prod is 160+ commits behind: `/terms` and `/privacy` 404, `/api/docs` open, no Finexer webhook receiver, old bundle id defaults, no linked identities, no tiers or metering. Steps: merge to `main` (Vercel auto-deploys the frontend), `railway up --service ai-wealth-dashboard --ci` and `railway up --service worker --ci` (needs acceptEdits mode), then set new Railway env vars (`FINEXER_WEBHOOK_SIGNING_SECRET`, `APNS_BUNDLE_ID=co.uk.auriqltd.sorted`, `APPLE_BUNDLE_ID` if set, `DEFAULT_TIER=max`, leave `OPEN_SIGNUP` unset). Verify each changed endpoint on prod with a minted token, not just `/health`. Owner: Kevin approves, agent executes. Blocks A2, A3, A4, Q5/Q6/Q7/Q10/Q11 answers. (done 2026-09-10, 047bb38)
  - note (2026-09-09, claude): Claims re-checked against live prod 2026-09-09: /terms and /privacy return 200 (not 404) and /api/docs is already 404, so those two lines are stale. /webhooks/stripe is 404, confirming recent work is not deployed. Everything else returns 401 from the auth middleware BEFORE routing, so an unauthenticated probe cannot tell deployed from missing; a real audit needs a prod session or the bot secret, which this VPS cannot mint (prod has its own session secret). A18 is now done, so the deploy is unblocked.
  - note (2026-09-10, claude): Block condition met 2026-09-09: A18 DNS resolves, A11 done, H17 done. scripts/release.py check is all green (env vars, both Railway services on release, Vercel, DNS, health, ancestry, MCP flag absent). Dry run would push main to release (213 commits), poll Vercel and Railway, run smoke checks and tag release-20260909-2251. Waiting on Kevin's go.
  - note (2026-09-10, claude): Release readiness re-checked 2026-09-10: scripts/release.py check is green on all nine enforced checks (env vars, both Railway services on release, Vercel 2d old and Ready, DNS, prod health 200, release is an ancestor of main, MCP connector absent). Not deployed by this session: G16 changed the Safe-to-Spend engine (card growth no longer subtracted, reserve kept only as a fallback) and merged to UAT at 00:16 today, so prod would ship a same-day money-maths change Kevin has not yet seen on his phone. Needs Kevin's go, or a look at UAT first.
  - note (2026-09-10, claude): Deployed to production 2026-09-10 by Kevin's go: release fast-forwarded to main at 2f5f5a3, Vercel and both Railway services deployed, health/homepage/terms/privacy/auth-wall all pass. NOT tagged: the 'mcp connector absent' smoke check expects 404 from /api/mcp but the auth middleware returns 401 for every unknown path, so it can never pass (see H26). Connector absence independently confirmed: the unauthenticated discovery endpoint is 200 on UAT and 401 on prod, and the prod privacy page has zero connector mentions.
  - note (2026-09-10, claude): Deployed and tagged release-20260910-1137 at 047bb38 on 2026-09-10 after H26 fixed the smoke check. All seven smoke checks pass; release and main match; connector absent in prod (discovery endpoint 401, zero connector mentions on the privacy page). Codemagic iOS prod build not triggered, no API token on this host, start it by hand.
- [x] **A2. Close the two open security items.** [owner: claude] [priority: p1] [unblocks: Q11] (a) `frontend/components/LoginOverlay.tsx` and `frontend/app/login/page.tsx`: dead legacy PIN login with a hardcoded PIN in source; delete both and any route/link to them. (b) `reconnect_expected` in localStorage holds a real account number and sort code; store only the connection id or a masked form. Then run a dependency audit (`npm audit` in frontend, `pip-audit` or `safety` in backend/.venv) and record the date and result in `SECURITY.md`. Needed for Q11. (done 2026-09-06, a8e544b2be7c390553b2d4d4bced54dc8990b0e0)
- [x] **A3. Retention sweeps.** [owner: claude] [priority: p1] [unblocks: Q10] `SECURITY.md` section 6 and `PRIVACY.md` section 8 promise two automated sweeps that do not exist: dormant accounts deleted after 12 months of inactivity, and a connection's data deleted 30 days after consent withdrawal or expiry if the user never pressed Disconnect. Build one nightly arq cron in `backend/app/workers/sync_worker.py` that reuses the existing account-deletion routine (`routers/profile.py` DELETE /account) and the existing disconnect routine. Tests with fake collections. Needed for Q10; remove the "to be implemented" note from SECURITY.md. (done 2026-09-06, a25ee28f8c145deef5cdd955a0b6aa7278be1e8e)
- [x] **A4. In-app agent disclosure line.** [owner: claude] [priority: p1] [unblocks: Q6] Q6 asks where customers are told AURIQ LTD acts as Finexer's agent. Add one line at the bank-connect step and the sign-in footer: "Account information is provided by Finexer LTD, authorised by the FCA. AURIQ LTD acts as Finexer's agent." Wording depends on A9 (FRN). Design: quiet caption, not a card. (done 2026-09-06, 0aaa9754ab2b3e6fd426b0190e3c7db6b33ca42a)
  - note (2026-09-06, claude): Finexer's hosted consent page already carries the footer 'Auriq acts as Finexer Ltd's registered agent. Finexer Ltd is authorised by the FCA ... firm reference number 925695' (verified in sandbox 2026-09-06). The in-app line for the step before it is still needed.
- [ ] **A5. FRN wording on the legal pages.** [owner: kevin] [priority: p1] [unblocks: Q6, Q7] Until the FCA register shows AURIQ LTD, `frontend/content/terms.md` section 2 and `privacy.md` section 1 say "registered agent" in the present tense. Interim text is drafted in the compliance doc under Q6. Kevin confirms with Finexer which wording to publish; keep `TERMS.md`/`PRIVACY.md` at the root byte-identical with `frontend/content/*`. `PRIVACY.pdf` and `TERMS.pdf` at the root are stale and cannot be regenerated on this VPS.
- [ ] **A6. Screenshots and recording for Q5 and Q6** [owner: kevin] [priority: p1] [unblocks: Q5, Q6] on production after A1: sign-in, connect bank, Finexer consent, bank auth, return, accounts and transactions, disconnect, delete account, plus the terms and privacy disclosure sections. Kevin captures.
- [ ] **A7. Penetration test.** [owner: kevin] [priority: p1] [unblocks: Q11] Now: run OWASP ZAP baseline against UAT and fix findings. Before public launch: CREST manual test of web app, API and both mobile shells (UK market 2026: about £3,750 to £8,000, 5 to 7 days; Precursor Security publishes from £3,750). Kevin books; record the scheduled date in Q11.
- [ ] **A8. Kevin-only inputs for the questionnaire:** [owner: kevin] [priority: p1] [unblocks: Q2, Q12, Q13] insurance details (Q12), last backup restore test date or run one (Q13), Penny agent mode timing (recommend: not before approval, Q2 says not live), confirm nothing else changed in the onboarding data-sharing list (Q9).
- [ ] **A9. Play Console record.** [owner: kevin] [priority: p1] [unblocks: Q4] Organisation account for AURIQ LTD (D-U-N-S), create app "Sorted", package `co.uk.auriqltd.sorted` fixed by first upload, Finance category, financial-features declaration (personal finance management), privacy URL `https://wealth.auriqltd.co.uk/privacy`. Needs an Android release signing config and AAB build (see C2). Gives the Q4 Play URL.
- [x] **A10. Web shell for production: a build flag (NEXT_PUBLIC_WEB_PRODUCT=off) that renders a 'Sorted is an app' page with store badges on every product route while /terms, /privacy, the Finexer and Google return pages and the landing page keep working; owner sign-in may stay allowed.** [owner: claude] [priority: p1] [unblocks: Q4] (done 2026-09-06, 82d0cc2b4857c5fa594375c0d045c4e1aa434979)
- [x] **A11. Create the API custom domain api.wealth.auriqltd.co.uk on the Railway web service and the DNS record, so the apps call Railway directly instead of the Vercel /api rewrite.** [owner: kevin] [priority: p1] [unblocks: Q4] (done 2026-09-09)
  - note (2026-09-09, claude): Duplicate of A18 (same custom domain plus DNS record). Do them as one action, or close one.
  - note (2026-09-09, claude): Same job as A18, completed by it on 2026-09-09.
- [x] **A12. Point mobile builds at the API domain: build:mobile:prod base and the Codemagic prod variable become https://api.wealth.auriqltd.co.uk, add the domain to CORS, keep the Finexer return URL on the web domain.** [owner: claude] [priority: p1] [unblocks: Q4] (done 2026-09-06, 7adf9c34f87d8f2e991f56e7a04200dd7f5eba38)
- [x] **A13. Switch Vercel's Production Branch from main to release before the first merge into main; then confirm so the docs branch can be merged.** [owner: kevin] [priority: p1] [unblocks: Q7] (done 2026-09-06)
  - note (2026-09-06, claude): Vercel production branch is release; docs branch fast-forwarded into main at cb9e1c1 on 2026-09-06
- [x] **A14. Own bank picker for Finexer: reuse BankPickerSheet in Finexer mode so the user picks the bank in our design and lands straight on Finexer's consent screen with the provider preselected; Finexer accounts reconnect through the Finexer link too.** [owner: claude] [priority: p1] [unblocks: Q5] (done 2026-09-06, b2096539ba4564012f846ecd739601377fd98d57)
- [x] **A15. Dependency upgrades from the 2026-09-06 audit: frontend next 16.2.4 to 16.3.4 (nanoid, postcss, sharp highs) and backend aiohttp, pillow, cryptography, starlette, pyasn1, python-multipart, idna, click; upgrade, run the suite, restart UAT, re-audit and update SECURITY.md.** [owner: claude] [priority: p2] [unblocks: Q11] (done 2026-09-06, 0c3d34de27a5c2fac741ada3779c9dd13a3bec77)
  - note (2026-09-06, claude): Opened from A2's audit (2026-09-06). Counts and fix versions are in SECURITY.md section 2, Dependency audit log.
- [x] **A16. Accounts Add menu: rename 'Finexer (beta)' to 'Add Bank' (it takes over tutorial-add-bank), rename the TrueLayer entry to 'Add Bank via TrueLayer' and show it only on UAT via a build-time flag (e.g. NEXT_PUBLIC_TRUELAYER_PICKER=on set on the VPS frontend and the ios-capacitor UAT workflow, unset on Vercel prod and ios-capacitor-prod); frontend/app/components/AccountsPage.tsx ~line 2505** [owner: claude] [priority: p1] (done 2026-09-08, 7080602e296825230270d3e4bab4b25b5702589d)
- [x] **A17. Gate the MCP connector behind MCP_CONNECTOR_ENABLED (default false): do not register the oauth and mcp routers in backend/app/main.py unless the flag is true, hide the Settings Connected assistants section and the privacy and terms AI assistants you connect sections unless the flag is true, so production ships with the connector absent as the Finexer answers state (planned, not live). Must land before A1.** [owner: claude] [priority: p1] [state: blocked: backend test suite failed: (done 2026-09-08, ce843ffb09622fdfa33df46373839759df446666)
........................................................................ [  4%]
........................................................................ [  9%]
........................................................................ [ 14%]
.................................] [unblocks: Q2, Q9]
  - note (2026-09-08, claude): Added by the coordinator on 2026-09-08 after F2, F3 and F5 merged unconditionally; A1 must not deploy until this is done.
- [x] **A18. api.wealth.auriqltd.co.uk has no DNS record (getent fails from the VPS, 2026-09-08) yet app/core/config.py defaults API_PUBLIC_URL to it, DEPLOY.md says Capacitor build:mobile:prod bakes it, and the Connected assistants card shows it as the MCP connect address; either create the record (Cloudflare or Vercel DNS to the Railway web service) or change the default and all references to the Vercel-proxied https://wealth.auriqltd.co.uk/api; prod is currently reachable only through the Vercel /api rewrite** [owner: kevin] [priority: p1] (done 2026-09-09)
  - note (2026-09-09, claude): Two paths: (a) Kevin creates the Railway custom domain and the DNS record, which also closes A11 and Q4's callback answer; or (b) Fable changes API_PUBLIC_URL and every reference to the Vercel-proxied https://wealth.auriqltd.co.uk/api, code only, no account work, which unblocks A1 immediately. C10 already bakes the proxied URL into prod mobile builds as an interim.
  - note (2026-09-09, claude): Done 2026-09-09: Railway custom domain added on the ai-wealth-dashboard service (CNAME api.wealth -> ppn808x4.up.railway.app plus the _railway-verify TXT, both in Squarespace DNS by Kevin); certificate issued in about 8 minutes; https://api.wealth.auriqltd.co.uk/health returns ok and /docs is 404.
- [x] **A19. Refresh the Q10 answer now the retention sweeps shipped (A3, task_retention_sweep runs nightly at 03:30): remove the [KEVIN] caveat that the dormant-account purge and the 30-day post-closure sweep are 'to be implemented', state they are implemented and operational, and drop the matching 'to be implemented' note from SECURITY.md section 6 so the policy and the answer agree** [owner: claude] [priority: p1] [unblocks: Q10] (done 2026-09-10, 8caa0aaa0991137b8c8cf43a735e6dbe5aa4c49a)
- [x] **A20. Clear the four questionnaire [KEVIN: ...] markers whose blockers have now shipped, in docs/compliance/finexer-agent-controls-2026-09.md: line 153 'true only after the production deploy' (A1 deployed 2026-09-10, tag release-20260910-1137), line 98 'export from the site after deploy' (the Terms and Privacy PDFs can now be exported from production), line 126 'we intend to tighten the tax explainer prompt before go-live' (G4 shipped 2026-09-06, so state what it does now), and line 174 'dependency vulnerability audit to be run and dated before submission' (A15 upgraded and re-audited 2026-09-06, so run a fresh audit and date it); verify each claim against what is actually deployed before rewording, keep every answer block under 2000 characters, and leave the six markers that genuinely need Kevin (lines 25, 87, 145, 175, 185, 203) in place** [owner: claude] [priority: p1] (done 2026-09-10, 7059311cadb94dd54a536b695e50b8fe96131505)
  - note (2026-09-10, claude): Re-verified independently before rewording. Found that a concurrent pass (A19/A21) had already landed near-identical wording for the four markers directly on main, but its Q11 answer block was 2007 chars, over this doc's own 2000-char limit; fixed to 1984 chars (commit d939029). The earlier rejection note ('A20's worktree had no changes') was inaccurate; this item's worktree carries a SECURITY.md update recording a fresh dependency audit dated 2026-09-10 (pip-audit: 0 advisories backend; npm audit --omit=dev: 1 new moderate, baseline-browser-mapping, in frontend production deps; no Critical/High shipped).
- [x] **A21. Q10 answer still carries the stale caveat 'true only after the production deploy'; production was deployed and tagged release-20260910-1137 on 2026-09-10, so remove the marker and sweep every other question's answer for caveats that the deploy has now made false** [owner: claude] [priority: p1] (done 2026-09-10, 95d63449934aaec51c1dd7d18187bc0c1c0af491)
  - note (2026-09-10, claude): Priority was set to p1 by the other coordinator session in error (it was aiming at its own newly added item and the ids had raced). Not a judgement about this item; adjust if p1 is wrong for it.
  - note (2026-09-10, claude): Conflict was a concurrent edit to Q11 on main while A21 was in review; merged both intents, all answers verified under 2000 characters. Resolved by the coordinator, 2026-09-10.
- [x] **A22. Root TERMS.md is out of sync with the published frontend/content/terms.md: it still carries a 'DRAFT for review, not yet published' banner and an older last-updated date, while the live /terms page serves the published text; reconcile the root copy onto the published one (as was done for PRIVACY.md) so the canonical repo copy matches what Finexer will read, and note that TERMS.pdf and PRIVACY.pdf at the root are stale and cannot be regenerated on this host** [owner: claude] [priority: p1] [unblocks: Q7] (done 2026-09-11, 5cf8209e923a4814237996954bfa0cd01c0e6e49)
  - note (2026-09-11, claude): Merged 2026-09-11 (5cf8209): root TERMS.md now matches frontend/content/terms.md apart from the two leading HTML comments. TERMS.pdf and PRIVACY.pdf at the repo root remain stale and cannot be regenerated on this host; the published /terms and /privacy pages are the canonical text Finexer should read.
- [x] **A23. Three questionnaire markers may be stale because the work they describe as open has since shipped: line 85 says the in-app Finexer disclosure line 'does not exist in the app yet' but A4 (in-app agent disclosure line) is done 2026-09-06; line 107 says two Privacy Policy updates 'are being made before submission' with [KEVIN: confirm and deploy] but C9 and the later policy work shipped and production now serves the PDFs; line 175 says 'the legacy PIN login and the localStorage account-number item are still open as of 2026-09-06' but A2 (close the two open security items) is done 2026-09-06, which would make the sentence 'Outstanding findings: none rated Critical or High. Two Medium items were closed before submission' true rather than pending; verify each against the deployed code and the live site, clear the markers that are genuinely stale, and leave any that are not** [owner: claude] [priority: p1] (done 2026-09-11, e50adc429a82c5886b5f52e0376099f3d7f35121)
- [x] **A24. Frontend production dependency has a new moderate advisory with a fix available (found by the A20 audit, 2026-09-10): npm audit --omit=dev reports baseline-browser-mapping, GHSA-w5vr-8v7q-w6rv, denial of service on invalid input, reached transitively through next's browserslist dependency; this is new since the 2026-09-06 audit which showed zero production advisories, and Q11 of the questionnaire now carries a dated audit result, so close it before submission and re-run the audit to update that answer; the three high advisories in the same run are dev-only tooling and never ship** [owner: claude] [priority: p1] (done 2026-09-11, b47830df1f725afb196182634f09549d23eec323)
- [x] **A25. Check every questionnaire answer against Finexer's actual character limit before submission: the document's own preamble says each answer block must stay under 2000 characters, but measuring whole sections gives Q2 2003, Q11 2020 and Q13 2383, all over; establish whether the limit applies to the answer text alone or the whole section as pasted into the form, then trim whatever genuinely exceeds it, and record the counting method at the top of the document so future edits are measured the same way** [owner: claude] (done 2026-09-11, d21248781f1efce14e65600614a5656a7cb442f6)

## B. Penny cost, tiers and billing

- [x] **B1. Shorten the grow cache.** [owner: claude] [priority: p2] `GET /grow` caches up to 6 h, `GET /safe-to-spend` 90 s, so Planning and Home can show different figures for the same gap (£1,053.91 vs £749 seen 2026-09-06). Align the grow cache TTL to the safe-to-spend one or invalidate it on sync. (done 2026-09-06, e2f5189fe88b080e5bae6feb3ccde7b5974969ff)
- [x] **B2. Bill display names in the due-list chip.** [owner: claude] [priority: p2] `services/penny_chips.py` `home_payday_due` prints "a card or account payment" for card repayments and own-transfers. Use the destination card or account display name ("American Express card payment"). Squarespace lines clean to "Worksp"; consider a merchant alias. (done 2026-09-06, 72290e0bbf8ceb5df61d21fc45c8461a22b5b1ef)
- [x] **B3. Tax chips still on the model.** [owner: claude] [priority: p2] `tax_pension_carry_forward`, `tax_salary_sacrifice`, `tax_gift_aid` return `kind: "llm"` and count as messages. Add registry entries (verified, general explanation) plus the user's own figure where the tax engine exposes one. Registry doctrine in `PENNY_TOOLS.md`. (done 2026-09-07, e4ea3d244e374869c011354c1547bad55f6af884)
- [x] **B4. Settings usage row.** [owner: claude] [priority: p2] Show "Penny messages used 37 of 150, resets 1 Oct" in Settings under Sign-in methods, from `GET /subscription`. (done 2026-09-07, 4969dddc1ad1fc525a513136f3b269c2bdbb32a5)
- [x] **B5. Billing.** [owner: claude] [priority: p2] Stripe subscriptions on the web first (`prices_gbp` in `core/subscription.py`), then App Store and Play subscription products, then the £2.99 top-up as a consumable in-app purchase writing `penny_topups`. Wire the "Available soon" rows in `components/MoreMessagesSheet.tsx`. Vercel is already on Pro (verified 2026-09-06). (done 2026-09-09, ce66b40ef2130ad5958f2adffa351620a6497ad4)
  - note (2026-09-09, claude): Kevin 2026-09-09: no Stripe account yet; build against Stripe test mode with a placeholder key so it is ready the day the account exists, nothing live
- [x] **B6. Statements-only free tier path.** [owner: claude] [priority: p2] Tier `statements` has `open_banking: False` and `statement_uploads_per_month: 3`; nothing enforces either yet. Gate bank connect and count uploads (PDF parser in `services/pdf.py`, pipeline `pdf_statement` in `llm_usage`). (done 2026-09-07, cf8391c6e2f8806827cbed773e7f4fbbb88b92a8)
- [x] **B7. Lite refresh cadence.** [owner: claude] [priority: p2] Tier `lite` says `refresh: "daily"`; the 4-hourly reconcile cron syncs everyone. Make the cron respect the tier's cadence. (done 2026-09-07, 3af8ece53c86c4be0aaa5a543fc3f726bafa61a1)
- [ ] **B8. OpenRouter organisation account.** [owner: kevin] Move from Kevin's personal account to an AURIQ LTD organisation with per-environment keys and spend limits; keep `data_collection: deny`; file their data processing terms for the sub-processor record. Kevin creates the account; agent rotates keys in env (never in git).
- [x] **B9. Cost dashboard.** [owner: claude] [priority: p2] Bot-only `GET /admin/llm-usage?month=` aggregating `llm_usage` by pipeline and by user, so real AI cost per user replaces the estimates in the pricing doc. (done 2026-09-07, db11bb3ab93e021d7ec24b08f41736ba9ec15838)
- [x] **B10. Surface tier 402 messages on bank connect: BankPickerSheet and AccountsPage handleConnectBank/handleReconnect discard the error detail, so OPEN_BANKING_NOT_IN_TIER and CONNECTION_LIMIT_REACHED never show their message; render detail.message inline in the sheet.** [owner: claude] (done 2026-09-07)
- [x] **B11. Penny top-up packs: replace the single £2.99 row with three packs (20 for £0.99, 100 for £2.99 'Most popular', 200 for £4.99 'Best value'), packs last 90 days and draw down after the monthly allowance, Move to Max leads after a second pack in a month; model in docs/pricing/tiering-unit-economics-mcp-2026-09.md section 9; touches core/subscription.py PENNY_TOPUP, GET /subscription, penny_topups schema, components/MoreMessagesSheet.tsx and its design preview** [owner: claude] [priority: p2] (done 2026-09-08, 286ecbabafc283c68211990123fe866f4ea28550)
- [ ] **B12. Penny action parity: every UI write action should be proposable in chat; inventory in docs/penny/action-inventory.md (93 actions: 7 covered, 29 partial, 56 gaps, ~20 of them proposed as deliberately UI-only in section 7 for Kevin to confirm); build order: (1) edit/delete twins for the 7 covered creates (planned, allocation, commitment, checkpoint, recurring skip/edit/rule), (2) preferences that change money maths (pay period, income, pension, child benefit, debt target/start, cover-plan exclusions, hide balances), (3) account/ledger writes (offline accounts, ledger entries, mirror rules, disconnect), (4) full card terms not just APR, (5) intents and insights context; all propose-only via penny_proposals, consent gate unchanged; also add the missing DELETE /penny/agent-consent api.ts method and a Settings revoke** [owner: claude] [priority: p2]
  - note (2026-09-08, kevin): Kevin confirmed 2026-09-08: the section 7 exclusions in docs/penny/action-inventory.md stand (uploads and receipt scans, delete account, grant consent and proposal confirm/cancel, miscategorised guardrail queue, Mono exchange, test push, dark mode, Spend widget order and Home pins stay UI-only). Scope of B12 is the remaining gaps and partials only.
  - note (2026-09-08, claude): 2026-09-08: stage 1 split out as B14 (edit/delete twins). Stages 2 to 5 stay here; section 7 of docs/penny/action-inventory.md still needs Kevin's confirmation of the deliberately UI-only list.
  - note (2026-09-08, claude): 2026-09-08: Kevin approved stage 2; split out as B15 (preferences that change money maths). Stages 3 to 5 remain here.
  - note (2026-09-08, claude): 2026-09-08: stage 3 split out as B16 (account and ledger writes). Stages 4 and 5 remain here.
  - note (2026-09-08, claude): 2026-09-08: stages 4 and 5 split out as B17. When B17 lands, B12's remaining open question is section 7 of the inventory (deliberately UI-only list) for Kevin to confirm.
  - note (2026-09-08, claude): 2026-09-08: all five build stages shipped (B14 twins, B15 preferences, B16 accounts and ledger, B17 card terms plus intents and insights); 46 propose and 20 read tools. Remaining on B12: Kevin confirms the section 7 deliberately UI-only list in docs/penny/action-inventory.md, then the last 38 gaps are either built or retired.
- [x] **B13. Penny consent revoke in Settings: consent is one-way today, the Penny section in app/settings/SettingsPage.tsx shows 'Setting things up is on' with 'To turn this off, ask Penny to stop in chat' but nothing calls DELETE /penny/agent-consent (routers/can_i.py:825) and lib/api.ts has no method for it; add revokePennyAgentConsent to api.ts, replace the copy with a real off control in Settings (confirm step, since revoking cancels any unconfirmed proposals), refresh preferences so PennyConversation's consent state updates, and give Penny a 'stop setting things up' path that routes to the same endpoint; split out of B12** [owner: claude] [priority: p2] (done 2026-09-08, 0e2745f2322d4f00f1bb571b81da4e658ca19c73)
- [x] **B14. B12 stage 1: propose edit and delete twins for the covered creates (planned, allocation, commitment, checkpoint delete, recurring skip, edit and clear override), propose-only through penny_proposals with the consent gate unchanged** [owner: claude] (done 2026-09-08, ce80b60919e29ec5b599445f7602513b97e3975d)
- [x] **B15. B12 stage 2: Penny can propose the preferences that change money maths (pay period, income, pension, child benefit, debt target and start, cover-plan exclusions, hide balances), propose-only through penny_proposals mirroring PATCH /preferences validation** [owner: claude] (done 2026-09-08, 330ca1a356749987c19520f2380d120e2595335c)
- [x] **B16. B12 stage 3: Penny can propose account and ledger writes (offline accounts create, update, delete; ledger entries add, edit, delete; manual-account rules create, update, delete; disconnect a bank connection; sync now), propose-only through penny_proposals mirroring the routers** [owner: claude] (done 2026-09-08, d8201394732deba2dd760ba8f91a69d6c59ef055)
- [x] **B17. B12 stages 4 and 5: Penny can propose full card terms (status, promos, balance-transfer offers, minimum-payment note, product key, usage, not just APR) and the intent and insight actions (record, preview and undo a trend intent; mark opened, save context, dismiss, pin and label an insight), propose-only through penny_proposals mirroring the routers** [owner: claude] (done 2026-09-08, 02b5d50891a5d7ce39493d527d834597e6f6a180)
- [x] **B18. Penny and the connected assistants cannot answer 'what changed and why' about any headline figure: nothing records history, needle_history_col exists but holds zero documents for every user, so when Safe-to-Spend moves the user has no explanation and neither engine can produce one; write a daily snapshot of the Safe-to-Spend components (spendable_now, bills_total, income_before_payday, lowest_projected_balance, buffer, commitments_reserved, allocations_reserved, card_growth_total, plus the bill list identity and totals) into needle_history_col with a TTL, add a deterministic diff service that names the biggest movers between two snapshots, expose it as a Penny read tool (what_changed with an optional since) and include that tool in the MCP read set under the existing scopes, and surface the one-line version on the Home card when the figure has moved materially since the last time the user looked; deterministic figures only, the model may phrase but never derive** [owner: claude] [priority: p2] (done 2026-09-11, 95c5430c54115194eeed2767e3316919051a3149)
  - note (2026-09-11, claude): Scope: this pass ships the WRITE path only -- daily per-user snapshot (spendable_now, bills_total, income_before_payday, lowest_projected_balance, buffer, commitments_reserved, allocations_reserved, card_growth_total, bill-list identity) into a NEW collection safe_to_spend_history_col, not needle_history_col as the ticket's literal text suggested: inspection found needle_history_col already has a live, incompatible schema (one doc per CLOSED PAY PERIOD keyed f'{uid}:{period_end}', for the month-close reward narrative in services/needle.py) with real data (7 docs) and several existing readers -- reusing it would collide rather than add a clean read. Written by a new daily arq cron (task_safe_to_spend_snapshot, 05:00 UTC, registered like task_retention_sweep), TTL 90 days on computed_at (SAFE_TO_SPEND_HISTORY_TTL_DAYS), mirroring the MCP audit log's bound. NOT built in this pass: the deterministic diff service, the Penny/MCP what_changed read tool, and Home-card surfacing -- each is its own surface with its own tests/docs; today there is nothing to diff against yet since no history exists before this lands. Follow-up needs: (1) a pure diff function taking two snapshot docs and returning named deltas above a materiality threshold, (2) a Penny read tool wrapping it with an optional 'since' param, wired into the existing MCP read scopes, (3) a Home-card check-on-load comparing today's snapshot to the last one the user saw (needs a 'last seen snapshot' pointer per user) and a one-line surfaced diff when material. Verified the write path against real Mongo with a synthetic create-then-delete user id, and unit tests cover the fan-out's per-user isolation, idempotent upsert, and failure isolation (one user's compute error never blocks another's).
- [x] **B19. No way for a user to choose a plan: the five tiers exist as data and Stripe checkout, portal and webhooks all shipped in B5 behind BILLING_ENABLED, but the only plan surface is components/YourPlanCard.tsx with a Manage plan button that opens the Stripe customer portal and therefore only helps an existing subscriber; add a plan picker listing the five tiers with prices and what each includes, reachable from Settings and as a step in components/Onboarding.tsx after payday, defaulting a new user to statements; the picker itself needs no Stripe (choosing the free tier and viewing plans work today), moving to a paid tier calls the existing POST /billing/checkout and stays disabled with a clear message while BILLING_ENABLED is false, which is the case until Kevin's Stripe account exists (see B5's note)** [owner: codex] [priority: p2] (done 2026-09-11, a14d755f52c99c0b024f2fb7890ef7ccb3e5be88)
  - note (2026-09-10, kevin): Assigned to Codex by Kevin 2026-09-10; Claude sessions must not pick this up.
  - note (2026-09-10, claude): Codex design round committed on feature-B19-no-way-for-a at 5615b2c. The /design/your-plan preview now has three five-tier picker variants for Settings and onboarding, with light and dark modes plus billing off and on states. Variant A, the compact capability ladder, is recommended. TypeScript and check:design-index pass.
  - note (2026-09-11, claude): Preview corrected and recommitted at 506918e after rebasing onto current main. Read PRODUCT.md and DESIGN.md. npm run check:design-index and the full Next production build with TypeScript pass. Puppeteer verified A, B and C at 390px with a 4-second virtual-time hydration budget, HTTP 200, correct variant identity, working dark mode and no horizontal overflow.
- [x] **B20. Kevin needs to send offer notifications to users: there is no admin broadcast path at all, routers/admin.py only carries sync-all and fix-card-transactions and the notification catalogue in services/notifications.py is entirely event-driven per user; add a bot-only endpoint plus an /ops page section to compose an offer, choose an audience (by tier, by state such as at the Penny cap, or everyone), preview the copy and send it through the existing FCM and APNs path with a matching in-app card, recording each send so the same offer is never delivered twice and respecting the user's notification preferences; sending does not depend on Stripe, but any discounted price in an offer needs Stripe promotion codes, so keep the offer body free of prices until BILLING_ENABLED is true** [owner: claude] (done 2026-09-11, 1af93765a2bb9caf402ee9ad73a0b14dade3ea38)
  - note (2026-09-11, claude): health check failed for http://localhost:8000/health after retrying for 60.6s (timeout 60s, interval 2s)
- [x] **B21. Implement approved B19 variant A in Settings and onboarding with monthly, 3-month, 6-month and annual auto-renewing billing periods, plus a clearly disclosed 14-day introductory trial that converts to an annual subscription unless cancelled; extend Stripe checkout and subscription state without inventing discounts or prices, keep Statements genuinely free, and keep paid actions disabled while BILLING_ENABLED is false** [owner: codex] (done 2026-09-11, 942a66bb93c0f463f0eb52762e97b10b65d10ae5)
- [x] **B22. Trial and auto-renewal disclosure before billing is ever switched on (follow-up to B21, approved as built 2026-09-11): the 14-day trial converts to a 12-month subscription but the picker carries no copy telling the customer, at the point of starting it, that they will be charged the full annual price on a named date unless they cancel, and no reminder before that date; add prominent disclosure next to the trial control naming the amount and the charge date, a cancel-anytime line with where to cancel, a reminder notification a few days before conversion, and matching wording in TERMS.md section 9; UK consumer rules and both app stores expect the disclosure adjacent to the control, not in the terms alone; this must land before BILLING_ENABLED is ever true** [owner: claude] [priority: p1] (done 2026-09-11, b0093b5231df9de8aee0c95a4f292c7d5316c45e)
  - note (2026-09-11, claude): Kevin approved B21 as built on 2026-09-11 and asked for this follow-up. Hard gate: BILLING_ENABLED must not be set true in any environment until this is done.
  - note (2026-09-11, claude): Kevin refinement 2026-09-11: the longer billing periods carry no discount (3 months £50.97 = 3 x 16.99, 6 months £101.94, yearly £203.88 = 12 x 16.99), so there is no reason to prepay. Add a discount ladder and reflect it in TIER_PRICES_GBP, the picker's period buttons and the Stripe prices. Coordinator's proposed ladder for Kevin to confirm: 3 months about 5 percent off, 6 months about 10 percent off, yearly two months free (about 16.7 percent off); at Max that is roughly £48.42, £91.75 and £169.90, at Standard £28.47, £53.95 and £99.90. Show the saving on each button, for example 'Yearly £169.90, save £33.98'.
  - note (2026-09-11, claude): Kevin refinement 2026-09-11: the 14-day trial is annual-only. Coordinator's recommendation: make the trial available on every paid tier and every billing period, so the discount is the reason to prepay and the trial is never a lever into a 12-month commitment; the free Statements tier remains the permanent freemium. Kevin to confirm before build.
  - note (2026-09-11, claude): Pricing 2026-09-11 (proposed, Kevin to confirm; all endings .99 to match the monthly prices): Lite 5.99 / 16.99 / 31.99 / 59.99; Standard 9.99 / 28.99 / 53.99 / 99.99; Connect 12.99 / 36.99 / 69.99 / 129.99; Max 16.99 / 48.99 / 91.99 / 169.99 (monthly / 3 months / 6 months / yearly). Yearly is about 16.6 percent off on every tier, 6 months about 10 percent, 3 months under 2 pounds off (Kevin deciding whether 3 months stays). Show the saving in pounds on each period button and the per-month equivalent on yearly ('£14.17 a month, billed yearly'); do not claim 'two months free' at these prices because the yearly saving is a few pence short of two months on every tier.
  - note (2026-09-11, claude): Plan changes 2026-09-11: the billing provider computes the money, the server only mirrors state. Stripe (web): upgrades (higher tier, or shorter to longer period) apply immediately with proration_behavior=always_invoice, crediting unused time and charging the remainder, billing anchor reset to today; downgrades and longer-to-shorter period changes are scheduled for the end of the current period via a Subscription Schedule, no refund. App Store and Play (in-app): put the sixteen products in one subscription group ranked by tier then period; the stores handle upgrade proration and defer downgrades to renewal themselves, and the server learns of it from App Store Server Notifications v2 and Play RTDN. Server model: a subscriptions record per user (provider, provider_subscription_id, tier, period, status, current_period_start/end, scheduled_change {tier, period, effective_at}) written only from webhooks, plus a plan_changes audit log (from, to, requested_at, effective_at, proration_amount, actor). Entitlements read the effective tier: allowance rises immediately on upgrade with the used count carried over; on a downgrade that takes effect with more connected accounts than the new cap, do not disconnect anything, freeze the extras read-only and ask which to keep. Cancel keeps access to period end.
  - note (2026-09-11, claude): Kevin confirmed 2026-09-11: the .99 price table and the plan-change model above are agreed, not proposals; build to them. Still open for Kevin: whether the 3-month period stays, and whether the trial applies to every tier and period (coordinator recommends yes).
- [x] **B23. Resolving a broadcast audience writes to real user data: B20's resolve_audience calls get_subscription/penny_allowance per candidate user to evaluate the tier and state filters, and penny_allowance runs the idempotent _settle_packs bookkeeping, so merely PREVIEWING an audience mutates every candidate's pack records (disclosed by the B20 agent 2026-09-11 after running the live counts); a preview must be side-effect free, so split the read of a user's tier and allowance from the settlement write, or resolve the filters from stored fields rather than through the allowance path; add a test asserting resolve_audience performs no writes** [owner: claude] (done 2026-09-11, eccd7e67ba17cef3901f2f81182e3bc73c5b580e)
- [x] **B24. Test-fixture residue is inflating the broadcast audience on UAT: B23's live check found the 'everyone' filter resolving 12 recipients against B20's 11, because accounts with no subscriptions doc fall back to DEFAULT_TIER=max and are counted; fixture-engine-2@test.local still holds a preferences document and would receive a real broadcast, and _all_user_ids appears to enumerate identities that have no user record at all (not-the-owner@example.com was counted although it now has zero documents in every collection). Establish what _all_user_ids actually enumerates and exclude non-real identities from broadcast audiences, so an offer can never be sent to a test fixture; do NOT delete anyone's data without Kevin's explicit go, since removing records from the shared UAT database is a write to another tester's account** [owner: claude] (done 2026-09-11, ce2a01476782e3c74a3896161145e9c9d4b5e567)

## C. Store publishing

- [ ] **C1. iOS rebuild** [owner: kevin] [priority: p2] via Codemagic `ios-capacitor` to pick up the ring, chips, cap and Apple linking. Then Kevin links Apple from Settings once (Sign-in methods, Link Apple ID) so Hide My Email signs into his account.
- [x] **C2. Android release signing and AAB.** [owner: claude] [priority: p2] `capacitor-spike/android/app/build.gradle` has no release signingConfig and no upload keystore; Play needs an AAB. Generate an upload keystore (kept out of git, path documented in DEPLOY.md), add the release config, `./gradlew bundleRelease`. Rebuild the debug APK to UAT too (`/var/www/wealth-downloads/wealth.apk`). (done 2026-09-09, dbcc53cefdadece87dee3e7049774132fce8c07c)
  - note (2026-09-09, claude): Kevin 2026-09-09: Fable generates the upload keystore on the VPS (Play App Signing model), kept in a gitignored keys dir, path and password location documented in DEPLOY.md; Kevin keeps a copy and adds it to Codemagic later
- [x] **C3. Build tag.** [owner: claude] [priority: p2] `frontend/lib/buildTag.ts` is a hand-edited constant ("build 2026-08-18c"). Derive it from the git short SHA or CI build number at build time. (done 2026-09-07, 5de5e0fbd8ec716f93553326ce668b2d057332f3)
- [x] **C4. iPhone-only target.** [owner: claude] [priority: p2] Capacitor targets iPad by default, which means iPad screenshots at review. Set `TARGETED_DEVICE_FAMILY` to iPhone in the Codemagic plist/pbxproj step. (done 2026-09-07, dfdc712597ce8ae671ef19957e45b2db6934077c)
- [ ] **C5. Store listings.** [owner: claude] [priority: p2] App Store: screenshots for 6.9 and 6.5 inch, description, keywords, support URL, App Privacy labels, age rating, review notes with a demo account (needs D2). Play: Data safety form, feature graphic 1024x500 (does not exist), screenshots. Icon master: `capacitor-spike/assets/icon.png`.
- [ ] **C6. Housekeeping in the portals.** [owner: kevin] Remove the "Sorted by Auriq - Demised" App Store Connect record, delete provisioning profile "Sorted AppStore A", consider revoking the manually created iOS Distribution certificate (only Codemagic's API-key certificate should remain), remove the old `co.uk.auriqltd.wealth` App ID and Firebase Android app.
- [ ] **C7. Apple relay email.** [owner: claude] Not needed until the backend sends email; if outbound email is ever added, register the sending domain in Sign in with Apple for Email Communication.
  - note (2026-09-10, claude): Checked 2026-09-10: the backend still sends no outbound email (no smtplib, SendGrid, Postmark, SES or Mailgun sender anywhere in backend/app), so the Apple private-relay domain registration remains not-yet-applicable. Re-open this the moment any outbound email is added, otherwise mail to Apple relay addresses will silently bounce.
- [x] **C8. Codemagic: add a production workflow variant (build:mobile:prod) alongside the UAT one so a TestFlight build against the production backend can be produced for Q5 evidence and for release.** [owner: claude] [priority: p1] [unblocks: Q5] (done 2026-09-06, 91735297a026aaba8223b7aa4e83b4501cee4a18)
- [x] **C9. Change the contact email in Terms, Privacy and Security to info@auriqltd.co.uk (TERMS.md, PRIVACY.md, SECURITY.md, frontend/content/terms.md + privacy.md, regenerate the PDFs)** [owner: claude] [priority: p2] (done 2026-09-08, 6261e12776ca2bc2578ed4360f5a6adb27638bbf)
- [x] **C10. Codemagic branch model: neither iOS workflow in codemagic.yaml has a triggering block and both publish to the same TestFlight app, so UAT and prod builds are told apart only by build number and can be started from any branch; add triggering so ios-capacitor builds on push to main (UAT API) and ios-capacitor-prod on push to release (prod API), publish them to separate TestFlight beta groups (UAT testers / Production), set NEXT_PUBLIC_BUILD_TAG per workflow so the app shows its environment, make ios-capacitor-prod bake the Vercel-proxied https://wealth.auriqltd.co.uk/api until A18 gives api.wealth.auriqltd.co.uk a DNS record, and later have scripts/release.py deploy trigger the prod workflow via the Codemagic API after tagging** [owner: claude] [priority: p2] (done 2026-09-08, 353a8243579e42736d914d9b83e8b7505c7deecf)
- [ ] **C11. Android Play pipeline to match iOS: today Android is a debug APK built by hand on the VPS and sideloaded from /downloads/wealth.apk while iOS goes through Codemagic to TestFlight; after C2 (upload keystore + bundleRelease) and A9 (Play Console record), add two Codemagic workflows android-capacitor (push to main, UAT API, uploads the AAB to the Play internal testing track) and android-capacitor-prod (push to release, prod API, uploads to the production track as a draft or staged rollout), using Codemagic's google_play publishing with a Play service-account JSON stored as a Codemagic secret, the keystore as a Codemagic code-signing asset, versionCode from BUILD_NUMBER and versionName from the release tag, mirroring the ios workflows' MOBILE_TARGET pattern; keep the sideload APK for testers without Play access; document in DEPLOY.md and hook the prod workflow into scripts/release.py alongside C10** [owner: claude] [priority: p2]
  - note (2026-09-09, claude): C2 finding 2026-09-09: capacitor-spike/android/ is gitignored, so the release signingConfig in app/build.gradle is local-only; the Codemagic android workflows must re-apply it after cap add android with a scripted step (like the iOS PlistBuddy steps) and load the keystore from a Codemagic asset.
- [ ] **C12. Back up the Android upload keystore off this VPS (capacitor-spike/android/keys/upload-keystore.jks plus keystore.properties, both gitignored, fingerprint in DEPLOY.md) and add it to Codemagic as a code-signing asset for C11; if this key is lost, Play uploads for co.uk.auriqltd.sorted can never be signed again without Google support** [owner: kevin] [priority: p1]

## D. Identity and sign-up

- [ ] **D1. Enable open sign-up on UAT** [owner: kevin] [priority: p2] by adding `OPEN_SIGNUP=true` to `backend/.env` on the VPS and restarting `wealth-api`, then test: new Google account, new Apple account with Hide My Email, dot-variant Gmail lands in the same account, explicit link claims an automatic link. Keep prod off until launch.
- [ ] **D2. Reviewer path.** [owner: kevin] [priority: p1] Store review needs a working sign-in without the allow list: either `OPEN_SIGNUP=true` on prod at review time, or a demo account added to `ALLOWED_EMAILS`. Decide and document in the review notes.
- [x] **D3. Orphaned relay accounts.** [owner: claude] [priority: p2] When an explicit link claims an automatic relay link, the empty account keyed by the relay email stays behind. Add a sweep or delete it at link time when it has no connections (reuse the account-deletion routine; never delete an account with data). (done 2026-09-07, fc2243c9413d047bad60de10aa841f67d4335677)
- [x] **D4. Rate limiting and mobile login state for replicas.** [owner: claude] [priority: p2] `routers/auth.py` `_pending` dict and the in-memory rate limiter assume one process. Move both to Redis before running more than one Railway replica. (done 2026-09-07, 8281bc664070139758f95c063a27a2d5cff0630b)
- [x] **D5. Sign-up allowlist managed in the app instead of the ALLOWED_EMAILS env var: today OPEN_SIGNUP=false means only addresses in ALLOWED_EMAILS (backend/app/core/config.py, Gmail dot-insensitive via resolve_allowed_email) can create an account or sign in, and changing the list means editing env on Railway and redeploying; add an allowed_signups collection with email, invited_by, created_at, status, a bot-only or owner-only POST/DELETE /admin/allowlist endpoint plus an /ops page section to add or remove addresses, make resolve_signin_email consult the collection as well as the env var, keep OPEN_SIGNUP=true as the public-launch override, and show a friendlier 'Sorted is invite-only right now' screen instead of the bare 403 Access denied** [owner: claude] [priority: p2] (done 2026-09-08, 224ad0763af0cb5d1bb983a661377b2ad2e6eecf)

## E. Platform and scaling (from the pricing doc, section 6)

- [ ] **E1. Atlas M0 to M10** [owner: kevin] [priority: p2] before real users; M20 or M30 by 10,000. Kevin actions in Atlas; agent updates connection string in env.
- [x] **E2. Railway Pro and replicas** [owner: claude] [priority: p2] once D4 is done; spread the 4-hourly reconcile across the window and check Finexer rate limits (10,000 connections is about 42 syncs a minute). (done 2026-09-09, 56835e59bcab9d3068665e3d01783d5531cfa47a)
  - note (2026-09-09, claude): Code half shipped 2026-09-09 (spread + 429 handling + /admin/sync-stats). Remaining for Kevin: Railway Pro, replicas on the web service only (worker stays single until leader election), confirm shared REDIS_URL and MONGO_URI. Finexer's real rate limit is undocumented; RECONCILE_MAX_PER_MINUTE=40 comes from the item's arithmetic and the new 429 counters will surface the true ceiling.
- [x] **E3. Vercel Pro** [owner: kevin] already in place (verified 2026-09-06), nothing to do. (done 2026-09-06)
- [ ] **E4. Backup restore test** [owner: kevin] [priority: p2] documented with a date (Q13).
- [ ] **E5. Railway Pro upgrade and replicas on the web service only (worker stays single until leader election); confirm REDIS_URL and MONGO_URI are shared after the upgrade; the code half (reconcile spread, Finexer 429 handling, /admin/sync-stats) shipped as E2 on 2026-09-09** [owner: kevin]

## F. MCP connector (start after Finexer production approval)

- [ ] **F1. Design sign-off** [owner: kevin] with Finexer in writing: read-only, user-directed disclosure, masked identifiers, revocation and audit log. Full design in the pricing doc section 7.
  - note (2026-09-08, kevin): Kevin 2026-09-08: design sign-off with Finexer should describe v1 as aggregates and verdicts only, no transaction-level data leaves Sorted through the connector; scopes accounts:read, plans:read, insights:read (transactions:read deferred).
- [x] **F2. OAuth 2.1 authorisation server** [owner: claude] with PKCE and dynamic client registration on the backend; consent page reusing Google or Apple sign-in; token storage with refresh and revocation. (done 2026-09-08, 6c1d1afa6022f1abdb342594a98b7cede0460579)
  - note (2026-09-08, claude): Built 2026-09-08. For Kevin to confirm: revoke cascades to the access/refresh pair and code reuse revokes every rotated descendant (stricter than RFC minimum); OAuth error redirects use standard RFC 6749 codes. Access tokens 1 h, refresh 30 days rotating; consent page at /oauth/consent, preview /design/oauth-consent.
- [x] **F3. `/mcp` Streamable HTTP endpoint** [owner: claude] [priority: p2] exposing `TOOL_SCHEMAS` read tools through `execute_tool`; output masking of account numbers, sort codes, IBANs, consent ids; scopes `accounts:read`, `transactions:read`, `plans:read`, `insights:read`; per-user rate limit and the tier's monthly call allowance (Connect 2,000, Max 5,000); audit log. (done 2026-09-08, 34fe765fc7ff4b6cbc2a23780aa88f3b35828027)
  - note (2026-09-08, kevin): Kevin 2026-09-08: v1 scope is the agent's read tools only (TOOL_SCHEMAS, never PROPOSE_TOOL_SCHEMAS) and NO raw transactions over the connector. Drop the transactions:read scope for v1; exclude search_transactions entirely and strip per-transaction rows from get_account_activity (top_transactions, first_transaction, last_transaction in _summarise_account_activity, penny_tools.py ~L1247) when called via /mcp; audit every other read tool's output for merchant-date-amount rows before exposing it (get_category_spend, get_recurring_payments, get_spend_verdict unresolved.largest, get_insights). Aggregates, verdicts and figures only.
  - note (2026-09-08, claude): Built 2026-09-08 (session-bearer auth until F2). Two choices for Kevin to confirm: JSON-RPC error codes -32000 (monthly allowance spent) vs -32002 (tier has no connector); tools/list returns the full 18-tool catalogue to any authenticated principal, scope is enforced only at tools/call.
- [x] **F4. Settings "Connected assistants"** [owner: claude] list with client name, last used, revoke. (done 2026-09-08, a65f2b37d0689e7e58a6d0d0c6187181c9ec3acc)
- [x] **F5. Policy updates:** [owner: claude] Privacy Policy section "AI assistants you connect", Terms clause, Finexer Q2 and Q9 disclosure. (done 2026-09-08, 3bb398dc72216cbe39095c3dbb6489dc7eb18391)
  - note (2026-09-08, claude): 2026-09-08: Privacy section 6 and the Terms clause shipped (PDFs regenerated). The Finexer Q2/Q9 disclosure half moved to the new Kevin-owned item above.
- [ ] **F6. Finexer questionnaire Q2 and Q9: disclose the AI-assistant connector (read-only, aggregates only, banking identifiers masked, per-user audit log, revocation path, no payment or write capability); wording in the pricing doc section 7 and Privacy section 6** [owner: kevin]
- [x] **F7. MCP per-user rate limit: core/ratelimit.py keys the /mcp rule (60/min) by IP, but Claude and ChatGPT call from shared egress ranges so users of the same assistant would share one bucket; key the limit by OAuth token or user id after auth in routers/mcp.py, add a per-minute burst limit plus a soft daily cap (e.g. 500 tool calls/day) so a runaway agent loop cannot burn the monthly allowance in an hour, return 429 with Retry-After, keep the IP rule only for unauthenticated OAuth paths** [owner: claude] [priority: p1] (done 2026-09-08, 93fb4ab006e30b182bfe5d01bbbd4949b6236d0e)
- [x] **F8. MCP hostname and tier-aware Settings card: ConnectedAssistantsCard.tsx hardcodes CONNECT_URL to the prod API host (shows prod on UAT); source it from NEXT_PUBLIC_MCP_URL, serve the connector on a dedicated hostname (e.g. mcp.wealth.auriqltd.co.uk) so edge rules and a later service split apply to that traffic only, and for tiers with mcp_tool_calls_per_month 0 (Statements, Lite, Standard) replace the connect instructions with 'Available on Connect and Max'** [owner: claude] [priority: p2] (done 2026-09-08, 0a4f2ebc174367ea6474736f7a7d1024159314b4)
- [x] **F9. MCP allowance display and call pack: show 'N of 2,000 calls this month, resets 1 Oct' on the Connected assistants card from mcp_calls_col (allowance is enforced in routers/mcp.py check_mcp_allowance but never surfaced), and add an MCP call pack (e.g. 1,000 calls for £2.99) using the same pack mechanics as the Penny packs in B11; note DEFAULT_TIER=max means everyone has 5,000 free calls until B5 billing lands** [owner: claude] [priority: p2] (done 2026-09-08, e5c8a72ffe31f0749b7c8b6d08c9a5cc7c3d31b9)
- [x] **F10. MCP-only service mode: extend the A17 flag so the same backend image can run with only the oauth and mcp routers mounted (e.g. MCP_ONLY=true), sharing Mongo and Redis, so a second Railway service on the mcp hostname can isolate assistant traffic from the app API when the first Connect customer or visible load arrives; no second service deployed yet** [owner: claude] (done 2026-09-08, 43bc6b5fbf9842bbe8faa1803cdf8ef9680d33a9)
- [x] **F11. UAT MCP connector cannot authorise: backend/.env lacks API_PUBLIC_URL and MCP_PUBLIC_URL so every OAuth discovery URL and the 401 resource_metadata header point at api.wealth.auriqltd.co.uk (no DNS); set API_PUBLIC_URL=https://uat.wealth.auriqltd.co.uk/api and MCP_PUBLIC_URL=https://uat.wealth.auriqltd.co.uk/api/mcp on UAT, and make MCP_WWW_AUTHENTICATE in core/auth.py build resource_metadata from API_PUBLIC_URL (path-preserving) instead of MCP_ORIGIN so it resolves behind the /api prefix; document in docs/ops/ENV.md; nginx root /.well-known/oauth-* proxying is Kevin's, outside the repo** [owner: claude] [priority: p1] (done 2026-09-09, 9390682)
  - note (2026-09-09, claude): session abandoned, branch feature-F11-oauth-naive-datetime discarded
  - note (2026-09-09, claude): Already fixed by commit 9390682 (2026-09-08 23:05, app/core/timeutil.as_utc); zero occurrences since. Closed without work.
- [x] **F12. MCP OAuth discovery: Anthropic's client tries /.well-known/oauth-authorization-server/api (404 via nginx), /.well-known/openid-configuration/api (404) then /api/.well-known/openid-configuration, which our auth middleware answers 401, so the connector reports Failed to start MCP authorization; serve /.well-known/openid-configuration unauthenticated with the same metadata as oauth-authorization-server plus OIDC-required keys, and add it to the open path list in core/auth.py** [owner: claude] [priority: p1] (done 2026-09-08, 46a65300a63e801107097b5c31b866183c4257f1)
- [x] **F13. MCP OAuth token exchange 500s: Mongo returns naive datetimes (Motor client not tz_aware) and oauth.py compares expires_at/revoked_at against aware datetime.now(timezone.utc) in the code grant (line 372), refresh grant, revocation and mcp.py bearer validation; normalise on read with a shared helper and add regression tests using naive stored datetimes** [owner: claude] [priority: p1] (done 2026-09-08, c52200b9f7fe85bba6726a50535563eb6800e51d)
- [x] **F14. MCP audit log growth and UI: mcp_calls rows are written per tool call with only a (user_id, year_month) index and no TTL (main.py ~line 300), so a Max user at 5,000 calls a month accumulates 60k rows a year per user and the Connected assistants card renders the whole current month inline under 'View activity' (components/ConnectedAssistantsCard.tsx, GET /mcp/audit); add a TTL index (expireAfterSeconds, e.g. 90 days, on the call timestamp) with the monthly allowance count preserved by a separate per-month counter document so expiry never resets usage, cap the inline list to the last 10 calls, and move the full log to its own paginated page like /transactions (per-client filter, date grouping, cursor pagination on the endpoint) linked from the card; state the retention in the Privacy Policy's AI assistants section** [owner: claude] [priority: p2] (done 2026-09-10, 6289bd9b91f88fdcd8f2c7a8fe45fa7bec1683de)
  - note (2026-09-10, claude): Blocked 2026-09-10 by a transient health check: integrate.py restarts wealth-api then polls /health once after 5s, and the API had not finished starting under concurrent build load. Merge was correctly reverted, no errors in journalctl. Returned to review by the coordinator; see H22 for making the check retry.
  - note (2026-09-10, claude): Integrate 2026-09-10 blocked this with 'health check failed for http://localhost:8000/health' and reset to ORIG_HEAD. Two integrate passes from different sessions were restarting wealth-api concurrently at the time; /health returned 200 immediately afterwards with all three services active and no ERROR lines in the journal, so the failure was the concurrent restart, not the branch. Returned to review for a clean retry.
- [x] **F15. Settings Connected assistants card should not carry the inline activity dropdown: drop the 'Every request is logged' expandable list and the Hide activity toggle, keep the retention line and the 'View full log' link to /mcp-activity as the single way in** [owner: claude] (done 2026-09-10, a7954b121c088efeb7d831522695d1c23aabb16f)
- [x] **F16. Penny cannot answer 'how do I connect the app as an MCP' (Kevin 2026-09-10): there is no explain-registry topic for the connector, so the question falls through to a generic reply; add an explain topic covering what the connector is, that it is read-only with no raw transactions, the URL to paste from the Connected assistants card, that sign-in is required, and which tiers include it (connect and max); registry doctrine in PENNY_TOOLS.md, copy rules apply (no em dashes)** [owner: claude] [priority: p2] [state: blocked: backend test suite failed: (done 2026-09-11, e603466b27084f549bf06a31e2391848b5392718)
........................................................................ [  3%]
........................................................................ [  6%]
........................................................................ [ 10%]
.................................] [state: blocked: backend test suite failed:
  - note (2026-09-10, claude): Re-verified by the coordinator 2026-09-10: ran integrate's exact command (with the spotlight deselect) against the branch worktree, 2073 passed 1 deselected. The earlier block was not the spotlight test, which integrate always deselects, so it was transient. Requeued.
........................................................................ [  3%]
........................................................................ [  6%]
........................................................................ [ 10%]
.................................] [state: blocked: backend test suite failed:
  - note (2026-09-10, claude): Integrate blocked this once with 'backend test suite failed'. Re-ran the full suite in the worktree against current main: 2073 passed, 1 failed, and the single failure is the known pre-existing flaky tests/test_spotlight.py::test_material_estimate_change_earns_return_with_reason that integrate normally deselects. Nothing wrong with this branch; requeued.
........................................................................ [  3%]
........................................................................ [  6%]
........................................................................ [ 10%]
.................................] [state: blocked: backend test suite failed:
........................................................................ [  3%]
........................................................................ [  6%]
........................................................................ [ 10%]
.................................] [state: blocked: backend test suite failed:
........................................................................ [  3%]
........................................................................ [  6%]
........................................................................ [ 10%]
.................................]
- [x] **F17. Connected assistants is invisible in every mobile build (Kevin 2026-09-10: present on the UAT website, absent from the UAT APK and, by design per A17, from the prod website): frontend/scripts/build-mobile.sh excludes .env.local and .env*.local from its scratch dir (lines ~133) and codemagic.yaml sets no NEXT_PUBLIC_ vars, so NEXT_PUBLIC_MCP_CONNECTOR can never reach a mobile bundle even when the build targets the UAT API which has the connector enabled; pass the flag explicitly for UAT mobile builds (npm run build:mobile and the ios-capacitor workflow) while leaving it unset for build:mobile:prod and ios-capacitor-prod, alongside the same treatment NEXT_PUBLIC_TRUELAYER_PICKER needs; relates to C10** [owner: claude] [priority: p2] (done 2026-09-10, 865ff1b7c052d146be6ab6883d919393b020696a)

## G. Design and copy follow-ups

- [x] **G1. DESIGN.md drift.** [owner: claude] Still describes the retired Out-vs-In gap line, the old four-tab nav, and the old three-tile Safe-to-Spend card. Update to the current surface map in `CLAUDE.md`. (done 2026-09-07, 39df8c22c9b9dfe477508f12f34473639e8aeab0)
- [x] **G2. Dead code in `app/planning/PlanningPage.tsx`** [owner: claude] (`PlansDock`, `CommitmentCards`, `computeDebtRow`, `computeGrowRow`) and the duplicated fetch path in `lib/useAllTransactions.ts`. (done 2026-09-07, 418491c1ccceacc4561b5093766d136e79b0d5ee)
- [x] **G3. Design index.** [owner: claude] `app/design/page.tsx` now lists `penny-usage-ring`, `planning-plans`, `spend-penny-flow`, `upcoming-plan`; keep it current for every new preview. (done 2026-09-07, 163c797845544fdefbe6d5a49c4fbe5979320ce1)
- [x] **G4. `chat.py` tax explainer prompt** [owner: claude] [priority: p1] [unblocks: Q8] has no product constraints (EIS/SEIS content invites investment-product suggestions) and no temperature; tighten before go-live (Q8 promises it). (done 2026-09-06, e24bd9fbe1ffbf0c1f6ec81b0bdb2bde709c2361)
- [ ] **G5. Penny agent mode** [owner: claude] (propose-only writes, consent-gated) stays off until Finexer approval; when it ships it is a disclosed change with a privacy update.
- [x] **G6. Safe-to-Spend cards-short state should show the net shortfall figure in the hero, not £0, with severity driven by the net position** [owner: claude] (done 2026-09-08)
  - note (2026-09-06, claude): Context (2026-09-06, Kevin + Fable): short_reason_for in backend/app/services/net_position.py splits short into bills (cash after set-asides <= 0, red, hero = full gap) and cards (cash positive, card growth pushes net below zero, amber, hero forced to £0 in frontend/components/SafeToSpendCard.tsx). Result: a £10 cash gap plus £100 on cards reads as £110 red, while bills covered plus £1,000 on cards reads as £0 amber, so the worse position looks calmer. Kevin's decision: show the net figure for the shortfall in the cards case too. Still to discuss when picked up: caption wording, whether chip severity escalates on the net figure or a threshold, and keeping bills-at-risk as its own red signal. Design change: propose to Kevin before building.
  - note (2026-09-07, kevin): Re-raised by Kevin 2026-09-07 with the worked example: bills covered with £71 spare plus £846 of unpaid card growth shows a £0 amber hero, whereas £10 short on bills plus £50 on cards would show £60 short in red. Bills covered plus £1,000 on cards should not read calmer than the smaller cash gap. Backend: card_growth_unpaid reserve in routers/analytics.py step 6c and short_reason_for in services/net_position.py; frontend clamp in components/SafeToSpendCard.tsx. Still to agree before build: caption wording, whether chip severity escalates on the net figure or a threshold, and keeping bills-at-risk as its own red signal.
  - note (2026-09-08, kevin): Decision reversed by Kevin 2026-09-08 after the research pass: keep the £0 hero in the cards-short state (the Simple Safe-to-Spend precedent) rather than introduce a net figure; the caption already carries the cash-left and card-growth amounts, and adding a third number on the hero was judged too many. Closed without code change. If revisited, the agreed shape was: net figure in ink, amber chip for cards, red only for bills at risk, caption leading with the safe fact.
  - note (2026-09-09, kevin): 2026-09-09: Kevin saw the mirror case (bills-short hero £803 = £42 cash gap + £761 card growth, red) and chose the cash-led rule instead of the net rule: hero never includes card growth, cards can only floor it at £0. Tracked as G14.
- [x] **G7. Spend shape card: make Fixed and Moved distinguishable, the four labels under the bar are all muted grey with no key to the bar segments and no hint what the words mean; tie each Cell label to its JOB_COLOR (dot or tinted label) and add a one-line meaning (Fixed = bills and commitments, Moved = money sent to savings, debt and own accounts) in components/SpendShapeCard.tsx and the matching hero in app/spend/shape/MoneyShapeHero.tsx** [owner: claude] (done 2026-09-08, ecc6a34b7099bbc046f47a4f2fd82a27e5d1df0a)
- [x] **G8. Spend category list: the Other row is tappable (OtherRowView in components/SpendVerdictView.tsx has onOpen) but omits the trailing ChevronRight that MajorityRowView renders, so the amount column is misaligned and the row looks unsymmetrical; add the same 14px chevron** [owner: claude] (done 2026-09-08, d3d336833caa6d8ca5660821807e510a9fd43f74)
- [x] **G9. Settings: the Penny messages usage row (B4) was placed inside the Sign-in methods card; move it to be the first row of the existing Penny section ('What Penny can do on your behalf') in app/settings/SettingsPage.tsx, same row geometry and '398 left' pill; when B5 billing lands it re-homes into a Your plan card with tier, price and top-up packs (B11)** [owner: claude] (done 2026-09-08, 89e24561e13b9b06d2897867043c54e74499d539)
- [x] **G10. Cards page: fold the retired debt page's per-card outlook into app/cards/CardsPage.tsx as a final section below The trajectory ('Where each card is headed'): one 'what it would take' line (£X more a month clears every carried card by <month>) above per-card rows showing 0% until / rate pill, +£/mo at your pace, clears <month>, interest, and the cleared-monthly note; data from GET /debt-plan (services/debt_narration.get_debt_plan_view, currently uncalled) or a trimmed summary field; drop the Penny paragraph and the As it stands card; design change, propose to Kevin before build; makes Planning's Details caption ('Repayment plans are under Details') true** [owner: claude] [priority: p2] (done 2026-09-09, 61eb1efe1b705d63d8a4845a237374abf98d6692)
  - note (2026-09-09, claude): Kevin 2026-09-09: proposal as two coded art-direction variants under /design for phone review, winner folded into CardsPage, then H12
  - note (2026-09-09, claude): Fable's read of the screenshots 2026-09-09: both clean at 390px; the lead line is the with-extra projection while rows are at-your-pace, so months differ by design; B's rail is 24 months, a card clearing later would fall off it (fixture does not cover).
- [x] **G11. Home brief cards: body text stops well short of the right edge (seen on Card season 2026-09-07) because DismissChip sits as a 44px flex column beside the text block in components/HomeBrief.tsx, reserving that width for the full card height; review every card there (AskGenericCard, CelebrationCard, CliffCard incl. rhythm-info, UnfundedMoveCard, IntentPaceCard, MoveCard, RhythmCard) and let only the headline row clear the chip (absolute top-right chip or header-row-only padding, as HomeInsightSpotlight already does with pr-8) so body, actions and rows span the full width; also format thousands in companion body copy (£1175 should read £1,175)** [owner: claude] [priority: p2] (done 2026-09-08, 8b062efd1a0cfbaee7004f139450edfc77da709a)
- [x] **G12. Home brief cards, follow-up to G11 (Kevin 2026-09-08 screenshot): body text now runs to 16px from the right edge while the text column starts 43px from the left (p-4 + 15px icon + gap-3); give the text column in CliffCard and every sibling card in components/HomeBrief.tsx a matching right inset (about pr-7) so left and right feel symmetric, keep the headline's chip clearance; also replace the arrow glyph in the companion 'Card season' copy (services/companion.py, '4% → 56%') with words, e.g. 'from 4% to 56%'** [owner: claude] [priority: p2] (done 2026-09-08, b6f8a7a36ed05d4b186146e0305bc7e24636b2c1)
- [x] **G13. Spend shape card, follow-up to G7: the new legend line SHAPE_LEGEND in components/SpendShapeCard.tsx (line ~152) has 'truncate' so it cuts off at 'sent to savin...' on a phone; let it wrap (text-pretty, no truncate) or shorten the copy to fit one line at 12px on a 390px viewport** [owner: claude] [priority: p2] (done 2026-09-08, 8c2378f4d4d1e7ba93bd685027919b0b6c0c01d0)
- [x] **G14. Safe-to-Spend hero is inconsistent about card growth: in the cards-short state (cash positive) the hero is clamped to £0 and card growth stays out of the number, but in the bills-short state (cash negative) the hero shows the NET gap with card growth added in (Kevin 2026-09-09: £42 cash gap + £761 unpaid card growth rendered as £803 red); make it cash-led everywhere: the hero is always the cash position (bills-short shows the cash gap, £42 short, red; cards-short stays £0, amber), card growth is never added to the hero and can only floor it at £0, one secondary line in both states ('£761 went on cards unpaid this period'), drop the 'What makes up the safety gap' bar and its two-figure breakdown, keep the engine's net safe_to_spend unchanged for Penny/can_i; components/SafeToSpendCard.tsx and services/net_position.short_reason_for; design change, propose to Kevin before build; resolves the G6 asymmetry from the other side** [owner: claude] [priority: p2] (done 2026-09-09, e6e6f7d9f8c5150261cc74886772d36cfd85c15e)
  - note (2026-09-09, kevin): 2026-09-09 follow-on: Kevin's period-boundary argument (card spend is not a this-period cash outflow) removes the cards £0 floor as well and moves the engine to cash-led; tracked as G16.
- [x] **G15. Clean credit card display names on the Cards page: /cards/story passes the raw account name through, so two NatWest cards both render as MASTERCARD and descriptors read as SMITH,JOHN A/MR and IBCM PLATINUM; apply a display-name pass (title case, bank prefix, last four digits to disambiguate duplicates) as proposed by the names=clean toggle at /design/cards-page** [owner: claude] (done 2026-09-10, 88c88e1587d9afefa355a93893fd0842a0d28bcc)
- [x] **G16. Safe-to-Spend goes cash-led in the engine too (Kevin 2026-09-09, supersedes the 2026-08-25 net-position rule): card spend never leaves cash in the period it is charged, so card_growth_reserved must stop being subtracted from safe_to_spend in routers/analytics.py step 6c and the cards-short £0 floor goes; the hero shows cash after bills and set-asides (£38), Penny/can_i/pace/spend_impact read the same cash figure, and card growth becomes a separate fact: one amber line under the hero worded from the asked card terms (carried: '£761 added to your cards this period'; cleared monthly: '£761 on cards, due around <forecast bill date>') and a separate field in Penny's facts so her answer is '£38 free until payday, and £761 has gone on cards this period' instead of a flat no; keep the reserve ONLY as a fallback when a card has growth but no forecast payment series at all (fail-closed for pay-in-full users whose bill is not yet learned); update short_reason_for (cards state becomes a signifier, not a short state), DESIGN.md net-position wording, PENNY_TOOLS.md facts, and the safe-to-spend hardening tests; design change, propose to Kevin before build** [owner: claude] [priority: p2] (done 2026-09-10, 2990c39daf9817c0f6ecde2a252bca839521332f)
  - note (2026-09-09, claude): 2026-09-09: proposal built then dropped at Kevin's request; preview and analysis removed from main (revert cbfb83d). The engine was NEVER changed, so Safe-to-Spend still subtracts card growth per the 2026-08-25 net-position rule. Item left open as an idea, not started. Integrate auto-marked it done on the revert merge; corrected here.
  - note (2026-09-10, claude): Merge conflict was an add/add on the g16-safe-to-spend design preview: an unrelated agent had committed a half-finished copy to main on 2026-09-09. Resolved by keeping the branch's version. Resolved by the coordinator, 2026-09-10.
- [x] **G17. Coming-up strip headline: 'The next 5 days carry 82%. Heaviest: Mon 14 Sep, £185 across 2 payments' (lib/comingUp.tsx computeDrop, front-loaded branch) makes the reader reconstruct the definition (share of the fortnight's bill total that lands by the day the running total passes 50%) and the day count changes every day as today advances towards the fixed crossing day; rewrite with checkable money figures and no percentage: '£315 of the £384 due this fortnight lands by Monday, £185 of it on Monday itself' (front-loaded) and a matching plain form for the spread-out branch; keep the money-between-accounts line separate; update the design twin at app/design/coming-up** [owner: claude] (done 2026-09-10, 8e7b2eb5704426be800b227dadd91496246a32dd)
- [x] **G18. Planning hero needs its own Full calculation: '£542 spare each month' (app/planning/GrowPanel.tsx ~line 589, view.surplus_monthly from routers/grow.py _cashflow, the median-smoothed services/cashflow.py helper) is shown with no explanation; add a collapsible ledger like Home's Safe-to-Spend card: typical income (median over the N months used, name the months) minus typical spending minus debt repayments where the Savings-tab rule subtracts them, equals spare each month, with a footnote that it is a smoothed typical month, not this pay period; expose the components in GET /grow so the frontend never recomputes** [owner: claude] [priority: p2] (done 2026-09-10, 28b3a158da5cd5f5ca7030f21f8dcc316fcbcd96)
- [x] **G19. Home Safe-to-Spend Full calculation is missing the deduction that produces the lowest cash before payday: the ledger opens on 'Cash available until payday' and only describes the step in a sentence ('After applying £X of bills and £Y of income in date order', components/SafeToSpendCard.tsx ~line 258); add explicit rows using fields already in the payload: cash now (spendable_now), minus bills before payday (bills_total), plus income before payday (income_before_payday), equals lowest cash before payday (lowest_projected_balance), then buffer, plans reserved and envelopes reserved as today; keep the date-order sentence as the footnote under that row** [owner: claude] [priority: p2] (done 2026-09-10, e4da02bab7712782368dda040ae83a09c6c8bc28)
- [x] **G20. Cards page 'What drove it' does not reconcile with 'New spend' (Kevin 2026-09-10, reproduced: new spend £1,298.68, drivers sum £403.67): routers/cards.py new_spend sums every debit on the cards while the drivers loop drops the hardcoded {Transfer, Debt} categories (£876.76 here, one Transfer-categorised card debit, very likely a balance transfer) and truncates to the top five (drops Eating Out £18.25); fix: exclude movement-kind categories from new_spend using the shared category-kind helper (services/categories, the file's own comment already flags the hardcoded set) and surface them as their own line in the movement headline ('£877 moved between cards'), add an 'Other categories' row so drivers always sum to new spend, and add a test with a Transfer debit plus six categories asserting the reconciliation** [owner: claude] [priority: p2] (done 2026-09-10, e376f8545a70a0c5f91d881ba50553a99e61a328)
- [x] **G21. Upcoming Full calculation: the 'Savings backup £1,213 not included' and 'Predicted bills use your last 90 days' lines sit 4px right of the ledger rows above them because they carry px-1 while the ledger rows have no horizontal padding (app/planning/PlanningPage.tsx ~lines 1687-1693 vs the rows at ~1655-1686); align them with the rows (drop px-1 or give the rows the same inset) and check the Home Safe-to-Spend ledger footer uses the same inset so the two cards match** [owner: claude] (done 2026-09-10, d6c8d25011bb9649fbe3b5f3f3afcbc30258f590)
- [x] **G22. Card display-name holder-name rule is too broad and deletes real card names (follow-up to G15, merged 88c88e1): _looks_like_holder_name rule 2 in services/card_names.py matches ANY three alphabetic words despite its docstring saying ALL-CAPS, and depends on the _PRODUCT_WORDS denylist being exhaustive, which it cannot be, so 'Sainsburys Nectar Dual' renders as 'Sainsbury's Bank' and 'John Lewis Partnership' as 'Johnlewis', throwing the real card name away; check _provider_already_named BEFORE the holder-name rules (a descriptor naming its own bank is never a bare holder name) and require a genuinely ALL-CAPS descriptor before the three-word path fires, keeping the comma-and-title rule and the session-name rule as they are** [owner: claude] (done 2026-09-10, b1047308a8b5f70c14318a4f7fb63789a1040798)
- [x] **G23. Fix G15 card-names holder-name rule destroying legitimate mixed-case card product names (Sainsburys Nectar Dual, John Lewis Partnership)** [owner: claude] (done 2026-09-11, b1047308a8b5f70c14318a4f7fb63789a1040798)
  - note (2026-09-10, claude): session abandoned, branch feature-G23-fix-g15-card-names discarded
  - note (2026-09-11, claude): Closing as done: the defect this describes (the holder-name rule destroying mixed-case card product names like Sainsburys Nectar Dual and John Lewis Partnership) was fixed and shipped under G22, commit b1047308a8b5f70c14318a4f7fb63789a1040798, on 2026-09-10. G23 was a duplicate id opened by an agent that lost its worktree to a concurrent integrate; no separate work was ever needed. Closed rather than left blocked so it stops reading as outstanding on the board.
- [x] **G24. Card balance change is overstated because movement-kind CREDITS are dropped while movement-kind DEBITS are counted: for Kevin's current period routers/cards.py reports delta +1175.29 but the true net change is +369.29, an overstatement of exactly 806.00; card debits total 1298.68 and credits 929.39, of which only 123.39 reaches payments because the 806.00 'PAYMENT - THANK YOU' credit on the M&S card is categorised Transfer and excluded, while the two balance-transfer debits (461.02 NatWest, 415.74 HSBC, 876.76 together) ARE counted; make the movement maths symmetric so movement-kind credits offset movement-kind debits, fix the Home hero secondary line which shows +1,175 under the caption 'Net balance change: purchases minus refunds and repayments' (SafeToSpendCard.tsx line 138) when 876.76 of it is balance transfers and not purchases, and add a test asserting debits minus credits equals the reported delta** [owner: claude] [priority: p1] (done 2026-09-10, edb23b67ae0efc0bd53eeec0d662c90c9d126f66)
- [x] **G25. Cards page 'What drove it' must map one to one with the Spend page categories: the drivers list truncates to the top five and lumps the remainder into an invented 'Other categories' row, which is not an app concept and currently hides exactly one real category (Eating Out, 18.25); show the real categories the Spend page uses, with the same icons and names, and no synthetic bucket** [owner: claude] (done 2026-09-10, 7c19d2238e4f04c4489bbe4ddb1e98c90db5095e)
- [x] **G26. Home Safe-to-Spend card: the space above and below the 'How we got' disclosure row is uneven (Kevin, 2026-09-10 screenshot). The disclosure container at components/SafeToSpendCard.tsx line 329 carries mt-3 plus a top border, and its summary is min-h-12 with items-center so the label sits about 14px below that border; the footer container at line 376 then adds its own mt-3 before its top border, so the gap under the label is about 26px against 14px above it. Even the two gaps up, and check the same disclosure pattern on the Upcoming and Planning Full calculation cards so all three match** [owner: claude] (done 2026-09-10, c844e8564eff7898ac42e2b4aec8651f9a8c6f1f)
- [x] **G27. A connected credit card is missing from the Cards page and its repayment is not counted: the M&S card (account_number 7134, provider MS, status connected, connection_id 16e11ebfc2da47b6) does not appear in GET /cards/story's per_card list and its only transaction this period, an 806.00 'PAYMENT - THANK YOU' credit dated 2026-09-01, is excluded from the movement maths, so payments reads 123.39 rather than 929.39; routers/cards.py skips a card when abs(card_delta) < 1 and balance == 0 (line ~154) which should not catch this one, so the exclusion is happening earlier in _credit_card_account_ids; establish whether this card is legitimately excluded (stale or duplicate connection, or a card belonging to another person) or whether a real repayment is being dropped, and make the reason explicit in code rather than incidental** [owner: claude] [priority: p2] (done 2026-09-10, ed543c6f35f6ee9b24b6f58b60a733e32eef6826)
- [x] **G28. Penny replies are cut off mid-word at exactly 60 characters (Kevin 2026-09-10 on prod: 'The £1,175 card balance growth this month came from new spen' and 'The £794 you moved to credit cards this period came from 3 p', both exactly 60 chars): services/penny_agent.py _parse_headline_reply_or_none line ~391 synthesises a fallback headline with first_sentence[:60] and no word-boundary trim whenever the model answers without the HEADLINE:/REPLY: labels; cut on a word boundary (or drop the synthetic headline entirely when the whole reply is one short sentence), and check why the card renders only the headline when the full text is returned as the body (components/PennyConversation.tsx); add a test asserting no synthesised headline ever ends mid-word** [owner: claude] [priority: p1] (done 2026-09-10, 343f4f7ea6ef0b843f2a98e7c8d70f57fb49af4c)
- [x] **G29. Inline Reconnect chip was never designed and makes every stale account row taller on both Home and Accounts (Kevin 2026-09-10 on prod, 8 rows needing reconnection): components/AccountLedgerRow.tsx line ~205 and components/AccountMiniCard.tsx line ~489 each add a Reconnect line inside the row, so the estate list on Home and the accounts list both grow by a line per affected account; propose a design that keeps the row height fixed (for example the amber signifier on the existing row with the reconnect action behind the row tap or a single grouped 'NEEDS RECONNECTING' action), variants for Kevin to pick on his phone per the design agreement, no in-place patching** [owner: codex] [priority: p2] (done 2026-09-11, a3e4f6bc5cbde60e74452ccac92e2e76292449b8)
  - note (2026-09-10, claude): An integrate pass blocked this with 'frontend build failed: Turbopack 172 errors'. The cause was environmental, not this work: a stray symlink frontend/node_modules/node_modules in the shared tree pointed into the F17 worktree, whose own node_modules symlinks back, so Turbopack could resolve nothing. Symlink removed and stale .next cleared; the shared tree builds clean. Reset to to-do.
  - note (2026-09-10, kevin): Kevin 2026-09-10: run this through the Codex outside-critic design pass too, same method as G31 (OpenRouter gpt-5.3-codex), and bring variants back to Kevin before building.
  - note (2026-09-10, claude): session abandoned, branch feature-G29-reconnect-chip discarded
  - note (2026-09-10, claude): No duplicate work started. Its design round was approved as variant A and the resulting implementation shipped under G33 at a3e4f6bc; G29 is therefore stale board state and can be closed by the coordinator.
  - note (2026-09-11, kevin): Closed by Kevin's instruction 2026-09-11: the design round shipped as G33 (a3e4f6b) and the branch is merged; stale worktree removed.
- [x] **G30. Retire the savings-plan block: Kevin saw 'Your savings plan, 0 of 7 done' on prod Planning and expected it to be gone; app/planning/GrowPanel.tsx still renders savings_plans docs with tick, delete-step and delete-plan wired, but nothing in the frontend calls POST /savings/plan or the milestones endpoint any more (api.ts's saveSavingsPlan and addSavingsPlanMilestones have no callers), so the block only ever shows legacy data (UAT has zero savings_plans docs, prod still has Kevin's); confirm with Kevin then remove the block, the unused endpoints in routers/savings.py and the savings_plans collection, or restore a creation path if it is meant to live** [owner: claude] [priority: p2] (done 2026-09-10, 38863fde9d7420651500f77e8df846ab33aa4b7a)
- [x] **G31. Planning hero card design round via the Codex outside-critic pass (Kevin 2026-09-10, assigned to codex): the new Full calculation ledger (G18) puts horizontal rules above and below the 'Full calculation' toggle and above the 'Short each month' total, which read as clutter on the hero; remove those rules and take the whole hero card through a Codex design critique (docs pointer in the session notes, OpenRouter gpt-5.3-codex, verify the CSS is actually applied and rewrite its copy against Kevin's rules), covering the headline, the SHORT EACH MONTH chip, the two grey caption lines and the ledger; propose to Kevin before building** [owner: codex] [priority: p2] (done 2026-09-10, cc46a092a81cf292135e811c55bf2fde6bb7a965)
- [x] **G32. Settings Penny section uses the generic Wand2 icon (app/settings/SettingsPage.tsx line ~825) where it should carry Penny's own mark from components/PennyMark.tsx; swap it, keeping the plain indigo IconChip tint rather than the indigo-to-violet gradient, because DESIGN.md's Penny Gradient Rule reserves that gradient for the Penny button itself (the existing comment at line ~819 explains why the gradient was avoided, that reasoning still holds)** [owner: claude] (done 2026-09-10, 4842bf579c6e8c215f0489ec7e793439a53ba2fe)
  - note (2026-09-10, claude): An integrate pass blocked this with 'frontend build failed: Turbopack 172 errors'. Not a fault in this branch: a stray symlink frontend/node_modules/node_modules in the SHARED tree pointed at /root/worktrees/feature-F17-mobile-connector-flag/frontend/node_modules, whose own node_modules symlinks back, so Turbopack reported 'points out of the filesystem root' and could resolve nothing. Left by F17's mobile-build verification at 14:38. Removed the symlink and cleared a stale .next; the shared tree builds clean again.
- [x] **G33. Implement approved G29 variant A: keep Home's existing top reconnect bar, remove inline reconnect actions, and group provider reconnects on Accounts** [owner: codex] (done 2026-09-10, a3e4f6bc5cbde60e74452ccac92e2e76292449b8)
- [x] **G34. Implement approved G31 variant A on the Planning hero with one verdict and a borderless signed calculation disclosure** [owner: codex] (done 2026-09-10, d8e614cca605d0a7ac3666cdc78d99e904d741c9)
- [x] **G35. Safe-to-Spend moved from £64 to £276 in five hours with zero transactions (Kevin 2026-09-10, both screenshots dated 15 days before payday, confirmed no transactions on the account since 2026-09-09): the £212 rise matches exactly the fall in the coming-up fortnight bill total, £384 down to £172, so the forecast changed while no money moved; seventeen items merged to UAT between the two readings, of which G24 touched services/net_position.py and routers/analytics.py, so establish whether a merged change moved the figure or a scheduled recompute produced a different forecast, then add a regression test that pins safe_to_spend against a fixed transaction fixture so a code change can never silently move a user's headline figure again** [owner: claude] [priority: p1] (done 2026-09-11, 49bda0efdce186c36fa68c1ef1f26feb25010288)
  - note (2026-09-11, claude): Investigation: no merged commit in the 09-09/09-10 window changes the safe_to_spend number itself. G16 (drops card_growth_reserved for cards with a learned repayment series) merged the night before the window; G24 explicitly preserves the reserve math (display-only field). Likely cause: a legitimate scheduled recompute (4h sync cron) reacting to new bank info -- a bill matched to a newly observed PENDING debit is structurally excluded from upcoming_bills, or recurring_judge (capped at 10 judgements/refresh) reaching a series for the first time on a later tick. No point-in-time snapshot survives to confirm which (closed by B18). Regression test added: test_safe_to_spend_pins_against_a_fixed_transaction_fixture in test_safe_to_spend_hardening.py, pins compute_safe_to_spend's full composed output against a fixed fixture; verified it fails when the G16 reserve formula is reverted.
- [x] **G36. Plan picker sheet: the close X and the 'Choose what fits' title scroll away with the content (Kevin's UAT screenshot 2026-09-11); pin the header row so the X and title stay visible while the tier list and period buttons scroll beneath it, matching how other sheets keep their close control reachable** [owner: claude] (done 2026-09-11, a0eb9f5efa26809f4266f75fff8189b52f94cd67)
- [x] **G37. Transaction sheet loses the bank account on the Transactions page (Kevin 2026-09-11): the same Monzo credit opened from Home shows '11 Sep, Monzo, Saving Challenge (2026)' but opened from Spend's category drill-in, which lands on /transactions, shows only '11 Sep'; app/transactions/TransactionsPage.tsx mounts TeachingSheet at line ~511 without the account prop and never loads accounts, while HomePage.tsx (~908) and SpendPage.tsx (~1184) both pass account={accounts.find(a => a.id === selectedTx.account_id)}; load accounts on the Transactions page through the shared accounts cache used by Home and pass the same prop, confirm every row from GET /transactions/search carries account_id (the miscategorised sheet's comment says the list endpoint does), and audit the remaining TeachingSheet mounts for the same parity** [owner: claude] [priority: p2] (done 2026-09-11, 2bebcc5f3dd8e1698b03f17378eb37fb221e1413)
- [x] **G38. Spend page design round: the 'Also running warm' mini-row places the pace chip on the same line as the amount while the 'Needs a look' card stacks the chip under the amount in a two-row grid (components/SpendVerdictView.tsx, NotableCard ~line 364 versus the warm mini-row ~line 596), and Kevin 2026-09-11 wants the whole period view looked at rather than the chip patched; run it as a design round with two or three coded variants under frontend/app/design/ covering the hero, the notable and warm tiers, the looking-normal list and the shape card, bring them to Kevin on UAT per the H31 loop, then implement the pick** [owner: claude] [priority: p2] (done 2026-09-11, 4f894946c2f584ea63f167dcdbbe99c233bbd457)
- [x] **G39. Mortgage and car finance need a category home now that the Insights page is gone (Kevin 2026-09-11): routers/savings_insights.py detects both by merchant keyword and, per its own comment at ~line 126, has no reliable category because the payments land in Bills or Other, so those two insights route to a bare /transactions instead of a category card and nothing on Spend can carry them; decision: add built-in categories Mortgage and Car finance with kind COMMITMENT in services/categories.py (no subcategories), feed the insight trigger keywords into the deterministic categoriser so the engine files them, route the two insights to /transactions?category=Mortgage and ?category=Car%20finance and let their Penny-noticed callouts sit on those category cards, keep TV licence inside Bills (its insight already maps there), migrate existing matching transactions once with the usual user-correction precedence, and update ENGINE.md's category catalogue, the category-kind tests and the money-shape Fixed bucket which inherits COMMITMENT** [owner: claude] [priority: p2] (done 2026-09-11, 9a86c60a12a8a951e5485931c5142c68cb77139b)
  - note (2026-09-11, claude): Verified after merge 2026-09-11: Kevin's safe_to_spend, spendable_now, bills_total, income_before_payday and lowest_projected_balance are all unchanged to the penny, and the backfill migration relabelled 0 transactions across ALL users, not just Kevin. Since savings_insights and the categoriser now share one trigger-keyword list, zero matches means no tester currently fires a mortgage or car-finance insight either, so the new routing to the category card is correct but untested against real data. Worth re-checking once a tester with a mortgage or car finance payment connects.
- [x] **G40. Plan picker period buttons: the price line and the new 'Save £x' line both sit at text-[10px], below the 12px Caption floor in DESIGN.md (reviewer minor on B22, 2026-09-11); bump both to 11px or Caption and check the four buttons still fit two per row at 360px** [owner: claude] (done 2026-09-11, dc2ad227bba8336ceb27cf77cd7260fbdd183728)
- [ ] **G41. Establish whether a predicted bill matching a PENDING debit should leave Safe-to-Spend's bills_total before it settles (leading hypothesis from the G35 investigation, which ruled out both code suspects with evidence): pending_transactions.py excludes a predicted bill from upcoming_bills/bills_total once it matches a freshly-observed pending debit, and task_reconcile_truelayer resyncs every four hours, so a bill going pending raises the headline figure with no settled transaction and nothing the user can see moved; confirm whether the pending debit has already reduced the account's available balance at that point (in which case the exclusion is correct and the figure is right) or has not (in which case the money is counted as free while still owed, and the bill should stay reserved until it settles); use the B18 daily snapshots once they have accumulated to name the exact field and bill that moved** [owner: claude] [state: blocked: awaiting Kevin's decision: (b) bill should stay reserved until it settles]
  - note (2026-09-11, claude): (b): bill should stay reserved until settled. TrueLayer docs: available = current + overdraft - pending; current does NOT net pending debits, only available does. Sorted stores current as accounts_col.balance (truelayer_sync.py:313-314, finexer_sync.py:603); Safe-to-Spend pools ONLY balance, never available (analytics.py:1175, 1115 _split_balances, via _safe_to_spend_accounts:3818). _match_pending_observed (analytics.py:3227) excludes the matched bill from bills_total once pending, on pending_transactions.py:5-16's premise that current/available both already reflect it -- true only for available. spendable_now (current) stays unchanged while bills_total drops: safe_to_spend rises by the full bill with nothing moved. No live example today: Kevin's only pending row is a GBP0 Klarna hold, matches no bill by amount; B18's safe_to_spend_history_col has 0 docs for every user. Recommend: stop excluding the bill in analytics.py:3213-3230, or prefer accounts_col.available (nets pending) over balance.
  - note (2026-09-11, claude): session abandoned, branch feature-G41-pending-bill-reserve discarded
  - note (2026-09-11, claude): Coordinator review of the 2026-09-11 investigation: its conclusion (b) rests on TrueLayer's documented formula (available = current + overdraft - pending) plus the fact that Sorted's spendable pool sums accounts_col.balance, which stores the provider 'current' field (truelayer_sync.py:313, analytics.py:1109-1122, projection at :1175 does not even fetch 'available'). But it found NO live example in Kevin's data (his only pending row is a 0.00 Klarna hold) and the B18 snapshot collection is still empty, and it directly contradicts the pending_transactions.py docstring, which says the opposite behaviour was CONFIRMED LIVE on 2026-09-01 via TrueLayer's pending endpoint, the phantom-shortfall bug the exclusion was written to fix. Banks differ: several UK banks move the ledger at authorisation so 'current' already nets pending. Do not act on documentation alone.
  - note (2026-09-11, claude): Decisive test before any change: for one real pending bill per provider on Kevin's accounts, capture accounts_col.balance AND the stored 'available' immediately before and after the pending row appears, and the settled drop afterwards; if balance is unchanged at pending time the exclusion double-counts and should be removed (or the pool should prefer 'available'), if balance already dropped the current behaviour is right and the 2026-09-01 fix stands. The answer may differ per bank, in which case the rule must be per-provider, not global. Safe-to-Spend is fail-closed by doctrine, so if this stays ambiguous the safer default is to keep the bill reserved until it settles.
- [x] **G42. Unfunded-move card recommends nothing, so the user loses the transfer suggestion exactly when the payment is overdue (Kevin 2026-09-11): services/companion.py builds the unfunded_move item at ~line 2638 purely as a notice, ending in the fixed sentence 'Top up the account, make the move if you already have, or skip it for this month', and never calls the source-finding logic that the sibling cover-plan 'move' card uses to say 'move £X from A to B'; the cover-plan engine only looks forward at what an account still has to cover, so once a planned own-transfer goes past due it drops out of that path and into this one; reproduced on the owner's data, £100 to American Express from Premier Current Account due 9 Sep, that account holds £44.68 so the gap is £55.32, while six other accounts could cover it; wire the same source finder into the unfunded_move branch, compute the gap between the source balance and the amount due, and offer the top-up transfer the way the cover-plan card does while keeping 'Skip this month' alongside it; rank current accounts ahead of savings and only offer a savings pot when nothing else covers the gap, since moving money out of savings to pay a card is a different decision; a source must still reserve its own obligations first, and the three simulations that must stay in lockstep per the shortfall engine apply here too; fall back to today's notice wording when no viable source exists, the same way the cover-plan card already has a no-viable-source branch** [owner: claude] [priority: p2] (done 2026-09-11, 53bdcd27d04be3fb027085a4c3dad80c2249e2f7)
- [x] **G43. Prefer the fewest transfers when suggesting sources (Kevin 2026-09-11, 'fewer moves is better'): G42 shipped the source finder into the unfunded-move card and on Kevin's data it suggests moving 70 across 3 accounts to cover a 55.32 gap, because each source is capped at its own headroom after reserving its bills and the picker fills legs in rank order rather than looking for a single account that could do it alone; change the leg picker so that within the existing ranking (current accounts before savings, a savings pot only when nothing else covers it, every source still reserving its own obligations first) it prefers the smallest number of legs, using one source when one has the capacity and splitting only when none does; note the picker is SHARED with the cover-plan move card since G42 extracted it, so this changes that card's suggestions too, which is the consistent outcome but must be stated and shown before and after; the three simulations must stay in lockstep** [owner: claude] (done 2026-09-11, c939385efcbcbb400d2e4f8ae908deb71389b74e)
- [x] **G44. Unfunded-move card names a count, not the accounts, so the user still cannot act on it (Kevin 2026-09-11, comparing against the old cover-plan move card he used to get): G42 wired the source finder into the unfunded_move branch but only surfaces a summary, the /today payload carries suggested_amount 70, suggested_from_count 3, suggested_from_name null and suggested_covers_all true with no per-leg list, so the card reads 'Moving £70 across 3 accounts' and never says which three; the cover-plan MoveCard already receives a move_map with per-source rows and renders them with the bank icon, the account name and the amount, ending in a 'Moving £X' total; send the same per-leg source list on the unfunded_move item and render it with the same row component so a past-due move looks and behaves like the forward-looking one, keeping the destination context line, the Skip this month control and the no-viable-source fallback; do not fork the row rendering, reuse what MoveCard already uses** [owner: claude] [priority: p2] (done 2026-09-11, af0dd5113237b6fb36fdfaa4061846f00b633eaf)
- [x] **G45. Cover-plan account exclusions do not persist (Kevin 2026-09-11: unchecked two accounts, refreshed, both were checked again): verified the stored value is an empty list both in the preferences collection and through GET /preferences, so the write never landed; three defects in app/settings/SettingsPage.tsx, first toggleCoverAccount at ~line 434 calls api.updatePreferences inside the setExcludedIds state updater, and a React state updater must be pure because it can be invoked twice or with a value the render then discards, so move the network call out into an effect or an event handler; second the call ends in .catch(() => {}) so a failed save is completely invisible, give it an error path that reverts the optimistic toggle and tells the user; third the effect at ~line 344 resets excludedIds from rawPrefs whenever rawPrefs changes, so any refetch that resolves before the PATCH lands silently reverts the choice, so reconcile against the in-flight write instead of clobbering; add a test covering toggle, save failure and refetch-during-save; the exclusions are read by services/companion.py at line 1380 and drive which accounts the source finder may use, so a silent failure here changes the money the app recommends moving** [owner: claude] [priority: p1] (done 2026-09-12, 5bf60ff4b91be17fa93f5ae17c5b5dc38242502d)
  - note (2026-09-12, claude): Kevin asked for this to be fixed on 2026-09-12 and it is raised to p1. All three defects confirmed still present after the G46 merge, at new line numbers in app/settings/SettingsPage.tsx: toggleCoverAccount is now at line 490 and still calls api.updatePreferences inside the setExcludedIds updater at lines 492-503, still ends in .catch(() => {}) at line 501, and the effect that resets excludedIds from rawPrefs is now at lines 402-405. G46 added refreshCoverPlan and a liveRoute reset to the same handler, so the fix must keep the cover-plan refresh working after a successful save and must not leave liveRoute null when a save fails. Note the identical anti-pattern appears in toggleNotifPref at line 506, so a notification preference save can fail just as silently; assess and report rather than silently widening scope.
- [x] **G46. Cover-plan source settings need a design round (Kevin 2026-09-11, assigned to codex): the control for choosing which accounts the cover plan may move money from is a bare checkbox list buried in Settings, it gives no sense of the ranking the engine actually applies (current accounts before savings before offline, a class only reached when every earlier class combined cannot cover the amount, then within a class highest headroom first with account id as tie-break, fewest legs preferred per G43, each source keeping a ten pound buffer and skipping any account that is itself short), and it never shows the consequence of excluding an account; run this as a design round with two or three coded variants under frontend/app/design/ that make the ranking legible and the exclusion consequence visible, review on UAT per the H31 loop, then implement the pick; the underlying persistence bug is tracked separately so do not rewrite that logic here, and the engine's ranking is settled so this round changes presentation only** [owner: codex] [priority: p2] (done 2026-09-12, e36a5b83d1be291c4b07817fd005fbcd2ec9519f)
  - note (2026-09-12, claude): Coordinator 2026-09-12: the recorded uat link is the design index; the direct variant links are https://uat.wealth.auriqltd.co.uk/design/cover-plan-sources?variant=a&state=all&mode=dark (and variant=b, variant=c), each returning 200 on UAT. State values are all / savings / short, mode is dark / light. Variant A 'Where cover money can come from', B 'Accounts Sorted may use' with an explicit ranked source order, C 'Protect where money stays'. Kevin picks with backlog.py approve G46 "<choice>".
  - note (2026-09-12, claude): KEVIN PICKED VARIANT A (2026-09-12), with two changes required before it goes into production Settings. (1) Weird empty space below the icon at the top: the shield badge sits in a left column beside the eyebrow, title and paragraph, so a gutter about 200px tall runs down the left under the icon on a 390px phone (CoverPlanSourcesClient.tsx VariantA, the card header block). Fix by putting the badge inline with the COVER PLAN SAFEGUARDS eyebrow on one row, then letting the title and the paragraph run the full card width. (2) Offline is not a class of account: an offline account can itself be a current account or a savings account, and offline only describes how it is managed, that moving money needs a manual transfer. Variant A shows 'Offline money' as rung 3, a peer of 'Current accounts' and 'Savings', which teaches the wrong model. Express offline as an attribute on the account row, a badge such as 'manual transfer', and place the account in its real class.
  - note (2026-09-12, claude): Linked engine question raised as G47 (owner claude): the source finder in companion.py ranks ALL offline accounts last as their own class, ignoring the account_type it already stores, so once this design stops presenting offline as a rung the settings screen and the engine will disagree. Whoever implements G46 should present offline as an attribute per Kevin, and G47 decides whether the engine's ranking follows. Do not change the ranking as part of G46.
  - note (2026-09-12, claude): IMPECCABLE AUDIT of variant A (2026-09-12, screenshots at 390 and 360 wide, both themes, all three states, DOM-measured). MUST FIX before this ships into production Settings. (1) P0, the card overflows the viewport with a realistic account name: in RouteSummary at line 359 the account-name span gets an empty className when not expanded, and VariantA always calls RouteSummary with no expanded prop at line 399, so with a name like 'Everyday household joint current account' the card grows to 408px inside a 360px viewport and the toggles, the USED FIRST badge and the second 760 figure are clipped and unreachable rather than scrollable. Give the pill's name span a bound in both branches, for example max-w-[9rem] truncate, or reuse the expanded min-w-0 flex-1 truncate and let the pill shrink.
  - note (2026-09-12, claude): IMPECCABLE AUDIT continued. (2) Kevin's icon gutter confirmed and measured at lines 384-395: the size-9 badge is items-start beside a column that wraps to eyebrow plus a two-line title plus a three-line paragraph, leaving about 95px of empty column under the icon at both 390 and 360 wide. Pair the icon with the eyebrow and title row only, centred against that single line, and move the description paragraph out of the two-column flex so it runs full width beneath, the way SettingsContext does at line 629. (3) The eyebrow 'Cover plan safeguards' at line 389 fails WCAG AA in both themes, 2.56:1 on white and 3.31:1 on the dark card, because the tokens are inverted: it uses text-slate-400 dark:text-slate-500 while the correct pairing text-slate-500 dark:text-slate-400 (4.76:1 and 6.14:1) already sits at lines 338 and 391. Swap it. (5) Worth doing: the Skipped pill at lines 311-313 measures 4.34:1 light and 4.04:1 dark, just under the floor; bump one step to slate-600 and slate-300.
  - note (2026-09-12, claude): IMPECCABLE AUDIT continued. (4) Kevin's offline point confirmed at the model level: SourceKind at line 25 and ACCOUNTS at lines 97-105 model 'offline' as a peer of current and savings, GROUPS at lines 132-139 then renders it as rung 3 with the same numbered badge as rungs 1 and 2, and the fixture even gives it a provider called 'Offline account', which cements the illusion that it is an institution. Keep the three-step waterfall, since that is a real engine rule, but stop modelling offline as a SourceKind: make it a boolean such as requiresManualTransfer on a current or savings account, name the third rung by its mechanism ('Needs a manual transfer'), and show each row with its real class plus a small manual-transfer badge, for example 'Cash reserve, savings, manual transfer'. Audit also noted the good parts: red is used only for the uncovered gap and a self-short account, copy is clean, dark mode is at parity, and the AccountToggle rows already truncate long names correctly.
  - note (2026-09-12, claude): approved: variant A, the live source ladder, with the icon gutter fixed, offline expressed as a manual-transfer attribute rather than a rung, and the impeccable audit's must-fix list cleared
- [ ] **G47. Cover-plan source ranking treats every offline account as one class ranked last, ignoring the account_type it already stores (Kevin 2026-09-12, raised from the G46 design round): companion.py class_specs at about line 1882 ranks current, then savings, then all offline accounts regardless of whether an offline account is itself a current account or a savings account, while manual_accounts_col already carries account_type and the code only inspects it to skip credit cards at about line 1509; decide whether offline should stay a last-resort class because reaching it needs a manual transfer, or whether an offline current account should rank with current accounts and an offline savings account with savings, with the manual-transfer cost expressed as a tie-break or a caveat rather than a class; the G46 design fix presents offline as an attribute rather than a rung, so if the engine keeps ranking offline last the settings screen will contradict the engine and one of the two must move; the three simulations must stay in lockstep** [owner: claude] [priority: p2]
- [ ] **G48. Home brief card family needs a design round (Kevin 2026-09-12, assigned to codex): components/HomeBrief.tsx holds eight card types sharing one layout (AskPaydayCard, AskGenericCard, CelebrationCard, CliffCard, UnfundedMoveCard, IntentPaceCard, MoveCard, RhythmCard) and the two movement cards have become the densest surfaces on Home after three behaviour changes landed in two days (G42 gave the unfunded-move card a recommended source, G43 made it prefer the fewest transfers, G44 made it name the accounts), so a card originally written as a one-line notice now carries an amount, a destination, a source, a due date, a skip action and a review action with no design pass over the result; the earlier width fix G11 showed these cards share their problems, so run the round across the whole family rather than one card, settling the shared grammar: where the dismiss control sits, how an action row reads, what earns amber under the Red Is Risk rule, how a money figure and its companion line are ranked, and how two or more cards stack on Home; two or three coded variants under frontend/app/design/ against real fixtures including the dense unfunded-move and cover-plan cases, reviewed by Kevin on UAT per the H31 loop, then implement the pick; the engine behaviour is settled so this round changes presentation only** [owner: codex] [priority: p2] [state: in-progress] [branch: feature-G48-home-brief-card-family]
  - note (2026-09-12, kevin): Reassigned from codex to claude by Kevin 2026-09-12, along with all Codex work; design rounds now run in Claude sessions using the frontend design skill.
  - note (2026-09-12, kevin): Reassigned back to codex by Kevin 2026-09-12: he asked for the cash-moves round and the Spend refurbishment to sit with Codex, superseding the blanket move to Claude earlier the same day.
  - note (2026-09-12, claude): Design stack: Impeccable 4.3.1 is the primary Operate-mode craft framework; Emil Design Engineering governs motion and interaction details; Web Interface Guidelines is the final accessibility and interface audit. PRODUCT.md and DESIGN.md remain binding, with the Calm Cockpit and Red Is Risk rules taking precedence.
  - note (2026-09-12, codex): Codex correction: the preceding design-stack note was written by Codex; the CLI defaulted its actor label to Claude. The recorded skill stack remains unchanged.
- [ ] **G49. Cover plan sources: the 'Manual transfer' badge in components/CoverPlanSourcesCard.tsx line 238 is 10px sentence case with no tracking, which is neither a valid Caption (needs 12px) nor a valid Label (10-11px must be uppercase with +0.05em tracking) under DESIGN.md; the 'Skipped' pill four lines below gets it right, so either add uppercase tracking-wide to match it or move the badge to 12px sentence case (reviewer minor on G46, 2026-09-12)** [owner: claude]
  - note (2026-09-12, claude): Hold for G51: that item reworks components/CoverPlanSourcesCard.tsx wholesale after Kevin rejected the shipped G46 layout on scale grounds, so fix this badge as part of implementing the G51 pick rather than separately, to avoid two sessions editing the same file.
- [x] **G50. Cover plan sources: an account with exactly zero spare headroom is skipped by the engine but shows a normal toggle instead of the Skipped pill, because coverPlanView() in app/settings/SettingsPage.tsx derives shortAccountIds only from the destination accounts of currently-active move items, so an account that is short without an active move card is never flagged (reviewer observation on G46, 2026-09-12); derive the skipped set from the same eligibility the source finder applies rather than from live move cards, so the screen and the engine agree** [owner: claude] [priority: p1] (done 2026-09-12, 755d5a764662812e643278afe4333c16ae79fc4c)
  - note (2026-09-12, claude): Hold for G51: that item reworks components/CoverPlanSourcesCard.tsx and app/settings/SettingsPage.tsx's coverPlanView after Kevin rejected the shipped G46 layout on scale grounds. This is the correctness half (the screen decides what is skipped from live move cards rather than from the engine's own eligibility, so an account with exactly zero spare headroom shows a normal toggle), so it must be carried into the G51 implementation, not dropped with the old layout.
  - note (2026-09-12, claude): G51 (search-first exceptions manager) shipped without wiring a real fix for this: the skip/short state the engine actually applies (headroom = source_capacity - 10 buffer, computed per-account inside companion.py's _find_legs_for_destination / _source_min_running, which needs live balances, assessable bills and window income) is not available as a standalone per-account field anywhere in the API today, and the existing cover_source_eligible flag on Account is a STRUCTURAL eligibility (excludes credit cards, Kenya/statement accounts) unrelated to a live headroom shortfall, so it cannot substitute. CoverPlanSourcesCard.tsx still takes shortAccountIds as a prop exactly as before (unchanged interface), sourced by the same coverPlanView() in SettingsPage.tsx that derives it from active move-card destinations, so the original bug (an account with zero spare headroom but no live move card shows a normal toggle instead of Skipped) is unchanged. Proposed fix: extract the per-account headroom computation in companion.py (source_capacity/_source_min_running, around line 1780) into a helper callable independent of an active shortfall, and expose it via a new lightweight endpoint, e.g. GET /companion/cover-eligibility returning {account_id: {short: bool, headroom: number}} for every non-credit account, computed the same way the source finder computes it for a live move; SettingsPage would then call this endpoint instead of deriving shortAccountIds from moves.filter(type==="move"). Not attempted in G51: refactoring the live recommendation engine's internals is a real backend change with regression risk, out of scope for a frontend scale/design item, and G45 was concurrently touching SettingsPage.tsx so the change surface there was kept minimal.
  - note (2026-09-12, claude): Kevin asked for this gap to be fixed on 2026-09-12, raised to p1 and dispatched. Scope note for whoever builds it: G51 left the card's shortAccountIds prop interface unchanged and did not touch SettingsPage.tsx, so the fix is a backend headroom source plus rewiring coverPlanView() in app/settings/SettingsPage.tsx, and should not need to change CoverPlanSourcesCard.tsx at all. Prefer extending the existing GET /today/cover-plan (added by G46, compute_today_items with persist=False) over adding a second cover-plan endpoint, so Settings does not pay two round trips on load.
- [x] **G51. Cover Plan Safeguards card does not scale past a handful of accounts (Kevin 2026-09-12, rejecting the shipped G46 design): components/CoverPlanSourcesCard.tsx renders every account in every class as its own always-expanded toggle row with no search, no collapse and no limit, so on the owner's estate of seventeen non-credit accounts the Settings section runs for several screens and includes savings pots holding nothing (Groceries, Transport, Round up, Holiday all at zero), and roughly a screen of explanatory prose sits above the first control (the safeguards panel plus a paragraph per class); rework so the common case is short and the detail is reachable, for example collapse each class to a one-line summary naming how many accounts are allowed and letting the user open it, surface only accounts the user has actually excluded plus a way to search, fold zero-balance pots away behind a disclosure, and shorten the standing prose to one line with the rest behind a help affordance; keep the ranking legible, which was the point of G46, and keep every safeguard true; use the frontend design skill and run it as a design round with variants reviewed on UAT per the H31 loop** [owner: claude] [priority: p2] (done 2026-09-12, d5869508465c27a46df0b2e80106005d074c465a)
  - note (2026-09-12, claude): Carries two open defects from the shipped G46 card that must not be lost in the rework: G50, the skipped set is derived from currently-active move cards rather than the engine's own eligibility, so an account with exactly zero spare headroom shows a normal toggle instead of the Skipped state; and G49, the Manual transfer badge is 10px sentence case, neither a valid Caption nor a valid Label under DESIGN.md. Also keep what Kevin approved in G46: the icon inline with the eyebrow, and offline shown as a manual-transfer attribute on an account of its real class rather than as its own rung.
  - note (2026-09-12, claude): Coordinator 2026-09-12: the recorded uat link is the design index; direct variant links, all nine variant/state combinations verified 200 on UAT plus dark mode: https://uat.wealth.auriqltd.co.uk/design/cover-plan-sources-scale?variant=a&state=all&mode=light and variant=b and variant=c; states all / savings / short, modes light / dark. Measured at 390 wide against the seventeen-account fixture: A 530px (classes collapse to one line, exclusions noted inline, two taps to full inspection), B 618px (search-first, shows only turned-off or skipped accounts, worst case 1248px when everything is excluded), C 508px (drill-in one class per screen, most height-stable at 400-530px across every state). The shipped card is roughly 1700px. All three already carry the G49 badge fix and the G50 skipped-set fix. Kevin picks with backlog.py approve G51 "<choice>".
  - note (2026-09-12, claude): KEVIN PICKED VARIANT B, search-first exceptions manager (2026-09-12). One implementation constraint from the round's own measurements: B is the shortest in the common case but the tallest in the worst case, reaching 1248px at 390 wide when every account is excluded, because every excluded account renders flat. Cap that: past about five exceptions, summarise as a count with a disclosure rather than listing all of them, so the screen cannot grow without bound. Keep the static two-step ranking strip (Current N of M, Savings N of M) that makes the order legible, keep search as the door into all seventeen accounts with a browse-all affordance for someone with no name in mind, and keep the G46 decisions Kevin already approved: icon inline with the eyebrow, and offline shown as a manual-transfer attribute on an account of its real class rather than as its own rung.
  - note (2026-09-12, claude): approved: variant B, the search-first exceptions manager, with the everything-excluded case capped so the list cannot grow without bound
- [x] **G52. Notification preference toggles can fail silently, the same triple defect G45 fixed for cover-plan exclusions (confirmed by the G45 agent 2026-09-12): in app/settings/SettingsPage.tsx toggleNotifPref at about line 536 fires api.updatePreferences inside the setNotifPrefs state updater, so the PATCH can go out twice or not at all; it ends in .catch(() => {}) so a failed save is invisible and the switch keeps showing the new value; and the rawPrefs sync effect at about lines 415-421 reapplies notification_prefs whenever preferences refetch, so a refetch landing mid-save silently reverts the choice; apply the same shape as the G45 fix (compute next in the handler, single call outside any updater, revert and surface the failure, and guard the sync effect against in-flight writes) and cover it with a test; this matters because a user who turns a notification off and sees it stay off may still be receiving it** [owner: claude] [priority: p2] (done 2026-09-12, 408ffbd0e108519fc87fe20cef50e5d63b4e7aa6)
- [x] **G53. Spend period round refinement after Kevin saw variant A on his phone (2026-09-12): the category amount is too large and the pace pill should sit UNDERNEATH the amount rather than beside it. Both come from one place, FigureBadge in frontend/app/design/spend-period-round/primitives.tsx, which renders figure and badge as an inline-flex row with the lead figure at text-[19px]; restack it so the badge sits under the figure, right aligned to it, and bring the lead figure down so the row reads as a category line rather than a headline number. Apply through the shared primitive so variants A, B and C all pick it up and stay comparable, leave the 'current' baseline untouched since it renders the real shipped components, keep every figure in the mono tabular treatment, keep amber for watch and never red, and keep tap targets at 44px. Re-screenshot every variant at 390px light and dark and give Kevin the links again** [owner: claude] (done 2026-09-12, b3b30679ed5e304a4d7d4f2c68ac5e844fe9f93d)
  - note (2026-09-12, claude): approved: Variant A, one ranked ledger (Kevin, 2026-09-12), with the restacked FigureBadge: badge under the figure, right aligned, lead figure at 16px
- [ ] **G54. Cover-plan exclusions are written as a full-array replacement by two writers, so genuinely concurrent writes can silently lose one side (raised from the G45 third review, 2026-09-12): both app/settings/SettingsPage.tsx and Penny's set_cover_plan_exclusions proposal (services/penny_tools.py:5442 via routers/can_i.py:1142) PATCH the whole cover_plan_excluded_accounts array with no compare-and-swap, and the version field G45 added only tells a READER which snapshot is newer, it does not stop a writer clobbering data it never saw; move to delta writes (addToSet and pull on account ids) or a version-conditioned update before Penny agent mode is enabled anywhere, since that is what makes the second writer reachable; note the G45 test that claims to cover this uses a Penny payload that is a strict superset of the Settings payload, so nothing is actually lost in it and it proves only that the counter advances** [owner: claude] [priority: p2]
  - note (2026-09-12, claude): Blocks nothing today because Penny agent mode consent is not enabled anywhere, which is the only thing making the second writer reachable; it must be closed before that changes. See G5.
- [ ] **G55. Structural cover-source eligibility is decided independently on the two sides, so the Settings list and the engine agree only by coincidence (raised from the G50 review, 2026-09-12): routers/accounts.py around lines 118-126 sets cover_source_eligible=True unconditionally for every TrueLayer and Finexer accounts_col document regardless of card type, so the only thing keeping a UK credit card off the Settings toggle list is the frontend's own string heuristic in app/settings/SettingsPage.tsx (type or subtype containing 'credit'), which has no guaranteed correspondence to the backend's is_credit_card_account() classification that companion.py actually uses when building source_capacity; make one side authoritative, preferably by having the backend set cover_source_eligible from the same classifier the engine uses and having the frontend trust it rather than re-deriving, and add a test pinning the two together; this is the same screen-disagrees-with-engine risk G50 closed for live headroom, but for structural eligibility** [owner: claude]
- [x] **G56. Dead line in the cover-plan eligibility tests (reviewer minor on G50, 2026-09-12): backend/tests/test_cover_plan_account_eligibility.py around line 408 in test_short_account_is_never_the_one_the_engine_actually_uses computes used_sources from move.get('legs') but the move item's real key is 'moves', so the expression always takes the else-None branch and the variable is never read again; delete the line, and while there consider whether that test's str(move) substring assertions should become structured lookups like the newer boundary test uses** [owner: claude] (done 2026-09-12, a84b9b37131106a0ac467fabf1fb3d2ac761194f)
- [ ] **G57. Spend page refurbishment round (Kevin 2026-09-12, assigned to codex): G38 fixed the pace chip alignment but Kevin wants a fresh look at the whole page for any refurbishment, not another point fix; the period view currently stacks a three-figure hero (Out leading, In and Moved secondary) plus a pace sentence, the This period and Patterns segmented control, a Needs a look tier, an Also running warm tier, a Looking normal list of eleven categories and the money-shape card, so the open questions are whether that many tiers earn their place, whether Out leading while Moved is the largest figure still reads honestly, how the eleven-category list should behave at length, and what the Patterns view is for once the shape card moved to its own page; two or three coded variants under frontend/app/design/ against the owner's real figures, reviewed on UAT per the H31 loop, then implement the pick; read PRODUCT.md and DESIGN.md first and name the design skill used in the item's note per H33** [owner: codex] [priority: p2]
- [ ] **G58. Child benefit toggle hides save failures (found while fixing G52, 2026-09-12): handleChildBenefitToggle in app/settings/SettingsPage.tsx around line 665 calls api.updatePreferences({has_child_benefit: next}).catch(() => {}), so a failed save leaves the switch showing a value the server never stored, with nothing told to the user; it does NOT have the impure-updater fault because the state call and the API call are not coupled, so the fix is smaller than G45 and G52: revert on failure, surface it with the ink-plus-amber-dot pattern those two established, and guard the rawPrefs sync effect for this field the same way; check the income and pension handlers in the same file at the same time and say whether they share it** [owner: claude] [state: in-progress] [branch: feature-G58-child-benefit-save-failures]
- [ ] **G59. Two adjacent save messages on the Settings page now use different styling for the same kind of event (reviewer observation on G52, 2026-09-12): the cover-plan coverSaveMsg in app/settings/SettingsPage.tsx around line 1346 still renders failure as text-red-700 dark:text-red-400 and success as text-emerald-500, a whole coloured sentence, while the notification notifSaveMsg two lines below correctly uses ink text with a small leading amber dot per DESIGN.md:142; red also reads wrong here because a failed settings save is not a financial risk, which the Red Is Risk rule at DESIGN.md:140 reserves it for; bring coverSaveMsg in line with the ink-plus-dot pattern, and check whether the success case needs a signifier at all or should simply be ink** [owner: claude]

## H. Repo hygiene

- [ ] **H1. Firebase config history.** [owner: kevin] `capacitor-spike/google-services.json` was tracked until 2026-09-05 and an old copy with the Android API key remains in git history. Decision: restrict the key to the Android package in Google Cloud console rather than rewrite history. Kevin.
- [x] **H2. Retired Expo project `mobile/`.** [owner: claude] `sync-shared.sh` still writes into `mobile/lib/shared/`; either delete the directory or stop syncing to it. (done 2026-09-07, 3637b867982d153d57fbb3f5ef0f35376b453b71)
  - note (2026-09-06, claude): session abandoned, branch item/H2-shared-sync discarded
- [ ] **H3. `docs/compliance` and `docs/pricing`** [owner: claude] are committed; keep `[KEVIN]` markers until answered, then remove them before the questionnaire is submitted.
  - note (2026-09-06, kevin): Board write-side smoke test
  - note (2026-09-10, claude): Checked 2026-09-10: docs/compliance and docs/pricing are committed, and every real [KEVIN] marker has already been answered and removed. The only remaining occurrence of the string is the preamble sentence in finexer-agent-controls-2026-09.md that explains the convention. Left open deliberately: the close condition is removing that preamble before the questionnaire is submitted, and it also carries the still-true caveat that answers saying production only hold once A1 deploys. Outstanding Kevin-only answers (Q2, Q12, Q13) are tracked on A8, not by markers.
  - note (2026-09-10, claude): CORRECTION to the 2026-09-10 note above, which was wrong: I grepped for the literal string [KEVIN] but the markers are written [KEVIN: <ask>] with a colon, so the pattern matched only the preamble. There are in fact 10 live markers in docs/compliance/finexer-agent-controls-2026-09.md at lines 25, 87, 98, 126, 145, 153, 174, 175, 185 and 203. Of those, four are now answerable without Kevin because the work they waited on has shipped (98 PDF export after deploy, 126 tax explainer prompt per G4, 153 the production-deploy caveat now A1 is done, 174 the dependency audit per A15); the other six need Kevin (25 MCP discussion date, 87 FRN wording per A5, 145 per A8, 175 pen test per A7, 185 insurance per A8, 203 final Finexer question). Raised as its own item.
- [x] **H4. Prune stale git worktrees on the VPS left by earlier sessions (git worktree prune, then remove directories whose branches are merged), without touching worktrees of live sessions.** [owner: claude] [priority: p2] (done 2026-09-06)
  - note (2026-09-06, claude): Eleven stale .claude/worktrees/agent-* checkouts removed; uncommitted work preserved on local archive/agent-<id>-<date> branches (not pushed): a1a5a4684775f8e2d, a310acffbcf81a57c, a56fe917b552cbc24, a7f9263e75657fe6c, a8297bf30170dc077, a9c315e208a8a5e04, aaa845c745f9e4d3b, ab2d9f14cfeb771cf, aba0c8ae823a72108, ae02340db730dd72e had uncommitted work; aff2fe65803aad26d was already clean.
- [x] **H5. Branch naming: feature branches are named feature-<ID>[-slug]; update session.sh, integrate.py, backlog docs and CLAUDE.md** [owner: claude] (done 2026-09-06, 68f568279ec58d8dd7c3db1263fcd73ff624b215)
- [x] **H6. Worktree sessions typecheck against the shared tree's shared/ package: frontend/node_modules is symlinked from the shared tree and node_modules/@wealth/shared resolves to /root/ai-wealth-dashboard/shared, so edits to a worktree's shared/src/types.ts are invisible to tsc there; make session.sh link or map @wealth/shared to the worktree's own shared/ directory.** [owner: claude] (done 2026-09-07, 420295e543f4dea192e601f4a359f6b89537e096)
- [x] **H7. integrate.py must reinstall dependencies when the merge changes lockfiles: run npm ci in frontend/ when package-lock.json changed and pip install -r backend/requirements.txt into the shared venv when requirements.txt changed, before the build and restart.** [owner: claude] (done 2026-09-07, afe1a10d2153ed334e82a5004ac79ec4ddb0b673)
  - note (2026-09-06, claude): Found during A15 (2026-09-06): the worktree's npm install replaced the node_modules symlink with a private copy, so main's node_modules stayed on next 16.2.4 after the merge and had to be refreshed by hand.
- [x] **H8. integrate.py restarts wealth-worker only when backend/app/workers changed, but the worker imports services (retention, penny_tools, sync services); restart the worker on any backend/app change so cron code never runs stale.** [owner: claude] (done 2026-09-07, b44dfc9d3a7945ace2d9d10f3ff0544ab1fb4d4c)
- [x] **H9. CLAUDE.md surface map says the Planning ladder shows a 'Cover this pay period' rung when the period gate is short; live code has no such rung (GET /grow emits done/active/locked only) and the period-short truth is the Planning hero line. Correct the surface map.** [owner: claude] (done 2026-09-07, 5b980678c3472a57c34d8c610e3e5a7272c53a43)
- [x] **H10. CLAUDE.md surface map still says the working tree is on docs/mobile-porting-checkpoint with the round uncommitted; the tree is on main and that branch is retired. Rewrite the surface map header to say main is integration and UAT, release is production, and work happens on feature-<ID> branches.** [owner: claude] [priority: p2] (done 2026-09-08, 0140a5afe0e427ce59a4554aff286e63ff53451e)
- [x] **H11. Remove or archive the retired Expo project under mobile/ (Capacitor wrap in capacitor-spike/ is the only mobile path); keep a git tag before deleting** [owner: claude] (done 2026-09-08, 17f551e9faefdaea4c034672a971ea482de42cda)
- [x] **H12. Remove the dead debt-page stack left after the 2026-08-05 legacy debt page deletion once the G cards-outlook item decides what it keeps: GET /debt-plan full view in routers/debt_plan.py and services/debt_narration.py (only /debt-plan/summary and commitments' _month_label_to_human are used), plus the DebtPlanView types already noted as removed in lib/api.ts; also rebuild the native app bundle, Kevin's phone still ships the retired page and the old four-tab nav** [owner: claude] (done 2026-09-09, 7d3d12c99ad99da11932dd461221d9399d9fbbf2)
- [x] **H13. Exclude frontend .env.local from the mobile build rsync and unset UAT-only NEXT_PUBLIC flags for MOBILE_TARGET prod, so a production mobile build made on the VPS never inherits the UAT-only env file the wealth-frontend build uses** [owner: claude] (done 2026-09-08, 2efd347f5892a1efdbf8b4a1dfc39cabca281170)
- [x] **H14. backlog.py add fails with 'HNN is not a known backlog item' when the title contains parentheses, an equals sign or an asterisk (seen 2026-09-08 adding H13); make the item parser and add tolerate punctuation in titles, with a test** [owner: claude] (done 2026-09-08, 559d9ad5989c9d3e8118a9552a423e0dea2ae10a)
- [x] **H15. build-mobile.sh must refuse to run unless its resolved project directory contains package.json and next.config.ts, and must never rsync when SCRATCH resolves outside that directory; a copy of the script run from /tmp on 2026-09-08 cd'd to / and mirrored the root filesystem into /.mobile-build before being killed** [owner: claude] (done 2026-09-08, 3c5a54c938750b2354529e897d85b3376641b85e)
- [x] **H16. Environment config manifest and drift check: there is no single record of per-environment variables (UAT reads backend/.env + frontend/.env.local via systemd; prod reads Railway services ai-wealth-dashboard + worker and Vercel), and DEPLOY.md's tables have drifted; add a checked-in manifest (names only, never values) with UAT and production columns covering every os.getenv in app/core/config.py and every NEXT_PUBLIC_ flag in frontend/lib/featureFlags.ts, plus scripts/env_drift.py that lists Railway variable names via the railway CLI and Vercel names via vercel env ls and diffs both against the manifest; first run 2026-09-08 found FCM_PROJECT_ID, APNS_KEY_ID, APNS_TEAM_ID and BOT_SECRET present on UAT but absent on Railway (production push silently unconfigured, admin and MCP audit routes uncallable), and Vercel unverifiable because the CLI on the VPS is not logged in** [owner: claude] [priority: p1] (done 2026-09-08, ec6f4fcc62d69b915b63324c1b888413b26328e8)
  - note (2026-09-08, claude): Vercel verified 2026-09-08 after login: project ai-wealth-dashboard, production and preview both carry only BACKEND_URL, matching DEPLOY.md. Railway names verified the same day (see item text). Remaining drift list: FCM_PROJECT_ID, APNS_KEY_ID, APNS_TEAM_ID, BOT_SECRET, MCP_CONNECTOR_ENABLED (intentionally absent), LINKED_IDENTITIES flag not enabled anywhere.
- [x] **H17. Production deploy pipeline is skewed: Railway services ai-wealth-dashboard and worker auto-deploy from main on every push (verified 2026-09-08, prod API at 4df6f86 = this morning's backlog commit, so every UAT integrate and every backlog commit rebuilds and restarts production), while the Vercel project ai-wealth-dashboard builds previews from main and its production deployment dates from 2026-07-28 (release branch tip b3ec620, 680 commits behind main); decide the model per docs/ops/BACKLOG.md (release = prod) and make both hosts follow it: point Railway's production environment at the release branch (or add a Railway environment for UAT-style main), keep Vercel production on release, and have scripts/integrate.py or a release script fast-forward release deliberately; until then no backlog commit should be able to restart prod** [owner: kevin] [priority: p1] [unblocks: Q4, Q10, Q11] (done 2026-09-08, 0298c46708eb6ce64c7a507a8a2aa290299894a1)
  - note (2026-09-08, claude): Fix is a Railway dashboard setting: for BOTH services (ai-wealth-dashboard web and worker) open Settings, Source, and change the deploy branch from main to release; then production only moves when release is fast-forwarded. Until then every merge into main deploys the backend to production. Coordinator, 2026-09-08.
- [x] **H18. release.py: the Railway branch check infers the deploy branch from the latest deployment's meta.branch, which cannot see a dashboard branch switch until something deploys from release (Kevin switched both services to release 2026-09-08 but the check stayed red); the Railway CLI token is rejected by the GraphQL API so the setting cannot be read; add --railway-branch-confirmed to check and deploy that downgrades those two items to AMBER, and make deploy verify after the push that both services produce a deployment with meta.branch == release and commitHash == the released sha, failing with the exact rollback command if only main deployments appear; update RELEASE.md and its prompt** [owner: claude] [priority: p1] (done 2026-09-08, 9cdb93a18b6a8c975bcab1a90babfe4a91205517)
- [x] **H19. finexer_sync.py list_providers() walks every provider page uncached on every consent sync; cache it (per process for 24 h, or a Mongo doc) so a sync costs the accounts, balances and transactions calls only** [owner: claude] (done 2026-09-09, 4d3a809dbb81790b4a84ceea72130ff9c6477df2)
- [x] **H20. AGENTS.md must carry the board and branch-per-item workflow for Codex agents, and the board needs a codex owner value: today AGENTS.md has no mention of scripts/session.sh, scripts/backlog.py or the integrate loop, so a Codex agent edits the shared tree directly and never records anything on the board** [owner: claude] (done 2026-09-09, cd62e26bd20b76d51bc5259ea3f3aefd15a4b2f2)
- [x] **H21. scripts/session.sh start <ID> silently attaches to an existing item when that id already exists and is done (seen 2026-09-09: start H18 reused a completed item and created a stray feature-H18 branch instead of erroring); make start refuse with a clear message when the id exists and is not in todo state, and make --title allocate a fresh id rather than assuming the caller guessed the next free one** [owner: claude] [priority: p2] (done 2026-09-10, d85cb228459109be6de2d6cd27e327cd1a443b33)
- [x] **H22. UAT tester tiers moved off the retired premium/free names onto the current model (2026-09-10, Kevin): everyone on connect, mbithi.maingi12@gmail.com on statements (free), kevin.maingi@auriqltd.co.uk on standard, applied via PATCH /subscription/admin/set-tier; before this every doc carried a retired name (9 premium, 1 free) so all ten fell back to DEFAULT_TIER=max** [owner: claude] (done 2026-09-10)
- [x] **H23. scripts/session.sh finish does not run npm run check:legal-content, so an edit to frontend/content/privacy.md or terms.md can reach review with the marker and section-renumbering contract broken; F14 hit this on 2026-09-10 by adding a Connector audit log retention row that survived the connector-off strip, putting a connector reference and a wrong Section 6 cross-reference into the policy production renders; add check:legal-content to the finish gate alongside the typecheck and check:design-index, and have integrate.py run it too** [owner: claude] (done 2026-09-10, be814f5e21f3a965637a699bf59adb96b4ddfea1)
- [x] **H24. scripts/integrate.py health check is a single poll 5s after the service restart, so a slow start under concurrent build load blocks and reverts a good merge (F14, 2026-09-10); retry the health check for up to 60s with a short interval before treating it as a failure, and do the same for the frontend check** [owner: claude] [priority: p1] (done 2026-09-10, b1d48701ac4e9b87720907a3d8db7d6db5f467d1)
- [x] **H25. Board needs a rejected state and swimlane: when a reviewer rejects work it currently has nowhere to go, so a branch sitting in review is treated as consent to merge by whichever integrate pass reaches it first (G15 shipped a known bug to UAT this way on 2026-09-10); add a 'rejected' state to scripts/backlog.py and the state machine, make scripts/integrate.py skip rejected items, give /ops/go-live a Rejected swimlane in both list and board views, and document the state and when to use it in CLAUDE.md, docs/ops/BACKLOG.md and AGENTS.md** [owner: claude] (done 2026-09-10, 32e47b9e54631cee88792eea21e183cc5c440cc8)
- [x] **H26. release.py deploy smoke check 'mcp connector absent' expects 404 from /api/mcp, but the auth middleware answers 401 before routing for EVERY unknown path (verified 2026-09-10: /api/definitely-not-a-real-route-xyz also 401), so the check can never pass and it aborted the 2026-09-10 release before tagging; test absence via the unauthenticated discovery endpoint instead (/api/.well-known/oauth-authorization-server is 200 on UAT with the connector on and 401 on prod with it off), and re-run deploy afterwards so the release gets its tag** [owner: claude] [priority: p1] (done 2026-09-10, 5141cf2890a3516063df49f9729bfebc8451c46c)
- [x] **H27. integrate.py block reasons embed raw multi-line command output into the TODO.md item line, which corrupts the board: on 2026-09-10 a Next build failure and a git push error were written verbatim into the G29 and G32 lines, breaking the item format, producing a merge conflict between two coordinator sessions and requiring a hand repair; truncate the reason to a single line (first line plus a character cap) before it reaches backlog.py, keep the full output in the integrate log or as a note rather than in the state tag, and reject or escape newlines in backlog.py's own block/reject reason handling so no caller can corrupt the board this way** [owner: claude] (done 2026-09-11, 8d176e859575bb723e6ee56b82db7dfde504c466)
- [x] **H28. tests/test_spotlight.py::test_material_estimate_change_earns_return_with_reason fails deterministically (3 of 3 runs, not flaky) and is permanently deselected by scripts/integrate.py, session.sh finish and every manual run, so it protects nothing and hides whatever it was written to catch; either fix the behaviour it asserts or delete the test with a note saying why, and remove the deselect from the tooling** [owner: claude] [priority: p2] (done 2026-09-11, fd941f0dba8906520ba8a0f376dc2bb4dc7907b5)
- [x] **H29. Each agent must only work items owned by its own model type: the board has carried a codex owner value since H20 but neither CLAUDE.md nor AGENTS.md tells a session to filter by it, so a Claude session can start a Codex item and the reverse; document the rule in both files and enforce it in scripts/session.sh start, which should refuse an item whose owner is not the caller's model type unless an explicit override flag is passed** [owner: claude] [priority: p2] (done 2026-09-10, 66c1e186a1c1054268b86f6022f860416d43d51f)
- [x] **H30. The design-variant protocol lives only in AGENTS.md and CLAUDE.md never mentions variants, so Claude sessions follow it only when a coordinator prompts them; write the full protocol into both files, including that the variants must exist and be linkable before an item is blocked for Kevin's choice (the B19 mistake)** [owner: claude] (done 2026-09-11, bd7fe3a366a0174ed9161a5120cbdb711b92a968)
- [x] **H31. UAT review swimlane so Kevin reviews real pages, not screenshots (Kevin 2026-09-11): a design round builds variants under frontend/app/design/<slug>/ but they are invisible until integrate merges the branch and rebuilds UAT, while AGENTS.md tells Codex to block the item with working preview links and never to run integrate itself, so the loop deadlocks (B19 is stuck exactly there: variants built and committed locally, branch unpushed, item still in-progress, and the link Kevin was given serves main's old page); add a 'uat' state to ITEM_STATES and STATE_RE in backend/app/services/backlog.py carrying a preview link, a 'backlog.py uat <id> --link <url>' command and an 'approve <id> "<choice>"' command that records which variant Kevin picked and moves the item back to in-progress with its owner unchanged so the SAME agent implements the winner on a fresh branch; make scripts/integrate.py land a design round in 'uat' instead of done, triggered by an explicit 'session.sh finish <ID> --uat-review' flag with a backstop heuristic when the merged diff touches only files under the frontend/app/design/ tree; notify Kevin when it lands, through the existing FCM and APNs path in services/notifications.py, with the preview link in the body; add a UAT column to the kanban and the filter bar on /ops/go-live next to the rejected swimlane from H25; document the full loop in docs/ops/BACKLOG.md, CLAUDE.md and AGENTS.md, replacing the AGENTS.md instruction that a blocked item must carry working preview links** [owner: claude] [priority: p1] (done 2026-09-11, 657b61ffd84908b83c4ec6bb919b456ac86d6070)
  - note (2026-09-11, claude): reopened: coordinator flagged that approve moves an item to in-progress but session.sh start only attaches to todo, so the loop deadlocks one step later; closing that gap in the same worktree
- [x] **H32. Worktree frontend builds fail under Turbopack ('Symlink node_modules is invalid, it points out of the filesystem root') because scripts/session.sh symlinks frontend/node_modules to the shared tree; it has now tripped three items (two 2026-09-10 notes and G36 on 2026-09-11, which verified with 'npx next build --webpack' instead) and leaves debris under the worktree; make session.sh either hard-link or bind the modules so Turbopack accepts them, or have the finish gate and the documented verification use the webpack build in worktrees, and record the chosen rule in CLAUDE.md and AGENTS.md** [owner: claude] (done 2026-09-11, 3a509781617538fe1e0cc0d43d699c5253a14be7)
- [x] **H33. CLAUDE.md's Design work section never names a design skill, so Claude design rounds rely on taste alone while the repo already pins design-taste-frontend, emil-design-eng, impeccable and web-design-guidelines in skills-lock.json (Kevin 2026-09-12, after rejecting a Codex-built Settings card that did not scale): document in CLAUDE.md which skill a Claude session invokes for a design round and when, propose design-taste-frontend for the art-direction variants themselves, emil-design-eng for interaction and motion polish, and web-design-guidelines as the accessibility and guidelines audit before Kevin sees anything, keeping the existing rule that an independent reviewer agent audits the diff against DESIGN.md first; state that the skill supplements PRODUCT.md and DESIGN.md rather than replacing them, since the Calm Cockpit north star and the named rules still win on any conflict; mirror the wording into AGENTS.md so both agents follow one protocol even though Codex work is currently reassigned to Claude** [owner: claude] [priority: p1] (done 2026-09-12, b2da712c39063cd38f2b40d1a12daebedd8b8bf7)
- [ ] **H34. The uat state records a generic design-index link instead of the preview it just landed, so the notification and the board send Kevin to a directory rather than the thing he is meant to review: G51 and G53 both landed 2026-09-12 with link https://uat.wealth.auriqltd.co.uk/design, when the actual previews are /design/spend-period-round?variant=a and friends. integrate.py's backstop knows exactly which paths under frontend/app/design/ the merged diff touched, so derive the slug from the changed files and record that URL; when a round has several variants, record the first one and let Kevin switch with the variant control the previews already carry; fall back to the design index only when no slug can be derived, and say so in the recorded link rather than silently degrading** [owner: claude]
